- iOS companion: home-screen widgets froze when the Operator deck replaced the classic dashboard as the home tab (the only snapshot publisher became unreachable) — the deck now publishes all App-Group snapshots on a throttle via `MillsWidgetPublisher.publishAll`; the Agents card counts only heartbeat-fresh active agents (spawn-husk registrations excluded) and the Queue card bounds ready plans to a 14-day recency window.
