- Added factory-model junction J2 (auto-harvest): the take-up reconciler now feeds the Pattern Loom taste gate automatically — a `plan-stamp-<slug>-…` plan that take-up advances to merged is longest-slug-matched against the approved catalog and recorded via `agent_pattern_record_instance` (green-instance count, auto-promotion, MR refs in provenance), with outcomes in `mills_takeup_pattern_harvests_total`. Engram verification stays safely deferred until the agent-context pod has a merged-instance checkout. (`pkg/mills/takeup/reconciler.go`, `pkg/mills/clients/pattern.go`, `docs/FACTORY_MODEL.md`)
