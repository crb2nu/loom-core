- **mills: registry-template runs get spec-aware work prompts** (`pkg/mills/workflow/runtime.go`). `agentPrompt` gave every non-legacy imperative run the crash-canary protocol prompt ("do not edit files; reply MILLS_CANARY_OK") — a live S7 registry run would have spawned an agent instructed to do nothing (the "S6-full will load spec-doc-aware prompts" deferred TODO, now due before first live use). Registry runs now derive a work prompt from the claimed backlog item (title + spec reference via an operator-wired lookup; best-effort — lookup failure degrades to a generic work prompt, never the canary protocol, never a failed spawn) with the v1 contract stated explicitly: work on the current branch, commit and push, no MRs, no merges — the run stops pre-merge and the item escalates for human review. Canary prompts are untouched replay contracts.
