- Distinguish late responses from unsolicited ones in the stdio mux. A reply that lands after the caller's context expired is the server finishing work the caller stopped waiting for; it is now metered as `IncMuxLateResponses` and logged at DEBUG with how late it was (`late_by`), while `dropped response (no pending waiter)` stays a WARN only for ids nobody ever sent. The 2026-09-13 daemon log carried 2,290 of those warnings in a day, all session-list replies arriving inside the same second the 3s budget fired (`pkg/transport/muxstdio/transport.go`, `metrics.go`).
