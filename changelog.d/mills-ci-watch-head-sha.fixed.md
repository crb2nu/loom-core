- **Bound `ci_watch` on merge requests whose head SHA never materializes** (`pkg/mills/clients/gitlab.go`, `pkg/mills/pipeline/dispatcher.go`, `pkg/mills/pipeline/error_class.go`, `pkg/mills/pipeline/runner.go`): `PollPipeline` now fails on a dedicated head-SHA deadline (and immediately when GitLab already reports conflicts or a non-open MR) with the new `ErrMRHeadSHAUnavailable`, classified `config` so the run escalates on first sight with the MR state and a rebase/repush remediation instead of replaying `MR <n> head sha pending` for the full 90-minute watch cap and blaming a pipeline that never existed.
