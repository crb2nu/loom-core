- `/api/mills/status` reports a `council_yield` block: finished council runs since one last produced a backlog delta, their cost, and when that last delta was (nil `last_delta_at` when the 50-run sample is exhausted). `loom mills status` renders it alongside the fields the operator already exposed but the CLI dropped: build sha, autonomy verdict with blockers, health-gate status and mode, capability-matrix rollup with non-green rows inline, both rolling-24h budget tiers, and last merge, with relative ages on timestamps.
