- **Mills shadow judge for the LLM-judged gates** (`FLEXINFER_SHADOW_JUDGE_MODEL` / `FLEXINFER_SHADOW_JUDGE_TIMEOUT`, loom-core #755): `gates.LLMGate` gains a `Shadow` judge that grades every `spec_conformance` / `pr_self_review` verdict concurrently with the primary and appends a `role=shadow` judgement — never touching `pass`, `judged_by` or `reasons`; errors and timeouts record nothing, and the canary/disabled/scoreless paths never consult it. The runner already persists one `judge.verdict` event per judgement, so the shadow lands in calibration for free. `guard.BuildJudgeCalibrationReport` now keys its rows on `(gate, role)`, the `mills_judge_calibration_mean_score` / `_discrimination` / `_graded_runs` gauges carry a `role` label (alerts must select `role="primary"`), the configuration-outcome `mean_judge_score` excludes shadow verdicts, and the HUD Mill Staff store maps the row's `role`. `buildShadowJudge` binds a FlexInfer client pinned to exactly the named lane (no registry or env fallback chain) and refuses `oa/`/`or/` gateway ids; `gates.RegisterLLMGatesWired` wires primary, tiebreaker and shadow together. This closes the executable-verification plan's measurement gap: a candidate local judge can now be compared with the frontier judge on live traffic before anyone flips the primary.
