- **Admit `libs/fi-fhir` and `libs/edilint` to HUD spawning for the Mills cross-repo lane** (`internal/hud/spawn_config.go`, `k8s/base/servers/mobile-hud/deployment.yaml`): `spawnProjectPaths` now maps `fi-fhir` and `edilint` to their `libs/` paths and `SPAWN_PROJECTS` lists both, so Mills items targeting fi-fhir reach `plan_slice` instead of failing admission with `project "libs/fi-fhir" is not configured for spawning` (loom-core #647). Pairs with the gitops policy change (platform/gitops!647) that added fi-fhir to `cross_repo.demand_projects`, `intake.gitlab.projects`, and the per-repo protected-path and budget overlays.
