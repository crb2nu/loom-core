- **Carry the escalation class as an argument instead of parsing it out of prose** (`pkg/mills/pipeline/runner.go`, `integrator.go`, `adapter.go`, `autonomy_gate.go`, `degraded_policy.go`, `preflight.go`, `escalate.go`): `escalateWithItem` now takes an `ErrorClass` and every call site declares what it just decided. `escalationClassFromReason` — which recovered the class by substring-matching `[class=…]` in a human-readable sentence the same function had just formatted — is deleted. `Integrator.escalateWithItem`, which never stamped a classification at all, now shares the Runner's write, so fan-out parents no longer escalate with a NULL class. The needle fallback survives only for a caller with genuinely no verdict, which closes a misclassification seam: escalation reasons embed arbitrary gate/agent text, so a test gate reporting `connection reset by peer` could classify a code failure as infra and make it auto-requeue-eligible. The `[class=…]` text remains in reasons as operator-facing prose.
