`flexinfer_rag` MCP tool (mcp-flexinfer): codebase RAG search over the embedded repo indexes (loom-core/loom/flexinfer/flexdeck) on the gfx906 retrieval plane via flexinfer-proxy `/v1/rag`. Default `retrieve_only=true` returns reranked chunk texts with path citations sub-second without touching a chat GPU lane; `retrieve_only=false` requests a server-side cited answer. Registered `always_allow` (read-only) in the MCP registry.
