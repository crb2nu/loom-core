- Harden Mills against MCP gateway stalls by serializing per-server request/response streams, adding recoverable live dependency health, bounding plan-slice scans, advancing dynamic workflow runs concurrently, and exposing dedicated hub, intake, and workflow telemetry; refresh the operator runbooks and `mills-ops` snapshot harness to match the live CLI, REST, and `mills_*` metric contracts.
