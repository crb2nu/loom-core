- The answerable floor: pipeline run detail carries a ground-truth `evidence` block (judge verdict chronology, provenance stamp, revert attribution banner) and the factory departure board shows the council's merged-work suppressions as dimmed rows via `GET /api/mills/demand-log` (`cmd/loom-mills-operator/handlers_demand_log.go`, `internal/hud/frontend/src/lib/components/mills/PipelineRunDetail.svelte`).
