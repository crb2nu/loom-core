- **operator + HUD: `GET /api/mills/escalations/relaunch-candidates`** (`cmd/loom-mills-operator/handlers_escalations.go`, `internal/hud/domain/mills/mills.go`). New open read listing escalated backlog items whose LATEST pipeline run recorded `EscalationRetryable=true` — the escalations a human can requeue without a policy override. Bare JSON array of `{ID, Title, EscalationClass, FailureClass, EndedAt}` rows (PascalCase store-row serialization), `[]` never `null` when empty. `?since` (RFC3339, default now-7d, windows the latest run's `EndedAt`) and `?limit` (default 50, max 200, capped) bound the list, newest ended first. Backed by `store.BacklogDAO.ListByEndedSince` (`pkg/mills/store/dao_escalations.go`); proxied through the HUD without an admin gate so the SPA fallback can never serve HTML to the poll.
