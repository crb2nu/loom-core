- Mills spawn git capture no longer unions session-telemetry paths into `FilesChanged` when the cumulative branch-vs-base capture succeeds: a gate-fail retry that reverts an out-of-scope file back to base leaves the path in telemetry but not on the branch, and the union made the scope gate false-fail the healthy retry and tripped the fabricated_slice truncated-patch heuristic. Gates now judge the merge envelope; telemetry remains the fallback when the capture yields no paths.
