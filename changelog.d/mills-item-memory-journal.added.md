- Mills pipelines gain durable inter-stage memory. The research stage's notes now reach the implement prompt (`cmd/loom-mills-operator/main.go`, default on, `LOOM_MILLS_RESEARCH_NOTES_IN_IMPLEMENT=0` to disable), and a per-backlog-item journal backed by `pkg/journalengine` records each stage outcome and renders as a stable prompt prefix (`pkg/mills/store/dao_item_memory.go`, `pkg/mills/store/migrations/017_backlog_item_memory.sql`, `pkg/mills/pipeline/item_memory.go`; default off, enable with `LOOM_MILLS_ITEM_JOURNAL=1`). `docs/JOURNAL_ENGINE.md` corrected to match code reality.
