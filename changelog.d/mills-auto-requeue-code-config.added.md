- **mills: opt-in context-carrying auto-requeue for code/config escalations** (`pipeline.auto_requeue.include_code_config`, default off). Shift data (2026-08-01): 39 of 53 weekly escalations were code/config-class — outside the sweep's classes — so their only unattended path was a council-minted sibling item (a council run's cost, hours of latency), even though 68% of escalations carry the classifier's own `EscalationRetryable=true` verdict and the fleet-enabled item journal already feeds prior-attempt context into retry prompts. The opt-in makes such runs eligible for exactly ONE unattended requeue, with every guard failing closed: requires the item journal active in the operator env (`LOOM_MILLS_ITEM_JOURNAL` — parity-test-pinned to the pipeline constant), the classifier's retryable verdict, non-zero spend on the escalated run (a $0 run is the !899 no-op-noise pattern; retrying it is a loop), and zero prior auto-requeues of any class for the item. Existing cooldown, fleet-day cap, per-tick cap, and budget gates all still apply. Eight-case eligibility test table; fleet enablement is a separate policy MR after the image lands.
