- Add the deterministic daily finished-goods digest (Finishing House S3): composed from the 24-hour shift report, appended once per UTC day as a `finishing.digest` event, delivered through the reconciler digest and mobile push hooks, readable and admin-rerunnable at `/api/mills/finishing/digest` (proxied by the HUD), with the `mills_finishing_digest_total` counter and runbook documentation.
