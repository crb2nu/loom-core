- Fixed the Operator Deck's "502 gremlins": `millsStore.fetchAll` now degrades per-source (`Promise.allSettled`) instead of redding every Mills surface when one endpoint fails, the first fully-failed tick renders as a quiet "reconnecting — showing last known state" (the operator's Recreate redeploy window is routine, not an outage), and the red `error` state seven panels share only fires after two consecutive fully-failed ticks. Added the factory-model line-health chip (first-pass yield · cost per bolt) to the Deck strip, and `docs/FACTORY_MODEL.md` — the consolidated map of the mill line, its feedback loops, and the missing junctions (with the pattern-coverage kill-test run 2026-08-01). (`internal/hud/frontend/src/lib/stores/mills.svelte.ts`, `components/operator/*`, `utils/operatorHelpers.ts`)
