- devbox: the shared Go module cache warm-up that serializes `go mod download` under `/gocache/.warmup.lock` before each quality gate is now POSIX sh + awk + flock instead of a python3 heredoc, so it actually runs in the Alpine sandbox image. Since 2026-09-14 04:29Z it had exited 127 on every gate (`quality gate preparation … sh: python3: not found`) and never serialized anything, which is how one pod could index the shared cache while another was still extracting a module. The warm-up tests now skip when `flock` is absent instead of when python3 is.
