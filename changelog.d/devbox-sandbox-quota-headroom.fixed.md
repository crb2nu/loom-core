- Keep devbox quota headroom for Mills tests gates: release each per-run sandbox (run and baseline) exactly once as soon as its gate returns, reap Mills sandboxes (`loom-mills-operator-*`) after `DEVBOX_MILLS_IDLE_TIMEOUT` (default 5m, no keep-warm grace) as the backstop for a gate that dies mid-call, and classify a ResourceQuota refusal (`is forbidden: exceeded quota`) as retryable `infra` attributed to `devbox_quota`, spaced a minute or more apart (1m doubling to 5m) instead of three attempts in one minute, and counted in `mills_devbox_sandbox_refused_total{reason="quota"}`.
