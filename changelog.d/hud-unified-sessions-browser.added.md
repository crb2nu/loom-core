HUD: unified cross-vendor session browser (Operations → Sessions) joining Claude Code + Codex desktop transcripts from every host with the live fleet. Transcripts now carry human titles (conversation summary / first prompt) and a kind tag (codex automation + subagent threads, claude sidechains) that the UI collapses as per-repo "N background runs"; transcript search matches what was said instead of raw JSONL and returns readable snippets; Operator Deck agent rows upgrade from hash ids to real conversation titles via exact POSIX-cksum linkage between transcript uuids/cwds and hook-minted agent ids.
