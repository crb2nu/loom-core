- Regression coverage for the HUD frontend coherence sweep's honest-failure and keyboard-containment guarantees: `InflightBoard` lane errors (error line shows, `lane.empty` suppressed, rows survive a poll blip), `OverviewPanel`'s `heroSummary` signal-loss rung across all five composed stores (and its precedence over every pressure branch), `MergeQueueSection`'s "✓ No branches in merge queue" being unreachable under a store error, `DataTable`'s j/k row cursor and the `stopPropagation` that keeps it from double-firing App-level sub-view shortcuts, `focusTrap`'s `dialogStore.openCount` contract, `ConfirmDialog`'s Escape-cancels-and-contains behavior, and `relativeTime`'s day tier and sub-10s window (the "72h ago" and round-vs-floor bugs the five deleted per-panel formatters carried). Adds a `dom` vitest project (happy-dom, `*.dom.test.ts`) for the mount-only event-wiring tests; the existing suite keeps the faster `node` environment.
