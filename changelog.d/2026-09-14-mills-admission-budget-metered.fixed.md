- Mills admission: the transactional pipeline, workflow, and council start kernels now count METERED spend against `max_usd_per_day` (pipeline `cost_usd - subscription_cost_usd`, council `cost_frontier_usd` only), matching the budget readout and `Budget.Allow` from the cost-attribution change. Before this the kernels summed total spend, so a day of subscription-billed Claude Code / Codex time ($79 list-price equivalent, $1.82 metered) stalled every admission with `daily USD cap 75.00 reached: spent 81.03` while four run slots sat empty.
