- **Mills overseer triage model knob** (`MILLS_TRIAGE_BACKEND` / `FLEXINFER_TRIAGE_MODEL`, loom-core #754): the backlog groomer's dedup/zombie verdicts and the foreman's LLM-composed issue bodies reused the rubric judge client and therefore ran 512-token JSON verdicts on the frontier judge (`oa/gpt-5.6-luna`). `overseer.Triage` gains a `Model` override and a single `chat()` dial-out tagged `component="mills-triage"` (new `clients.ComponentTriage`, so the traffic separates from `mills-judge` in `mills_llm_prompt_tokens_total`); `resolveMillsTriageClient` mirrors the judge/weaver resolvers: unset inherits the judge wiring byte-for-byte, `flexinfer` pins the proxy client on an explicit serving name (the warm 7900xtx lane), `litellm` binds the gateway, and any misconfiguration fails loud and falls back to the judge. An explicit triage model has no fallback chain, so a lane outage degrades the overseers to deterministic-only rather than to the frontier judge. Docs: `docs/MILLS.md` env table.
