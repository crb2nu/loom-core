- **agent-context: 12 zero-consumer MCP tools removed (93 → 81)** (`cmd/mcp-agent-context`, `pkg/agentcontext`). Each was verified to have no Go caller, no proxy-profile entry, and no documented human/LLM use before deletion. Deprecated aliases that only forwarded to the unified recall internals: `agent_context_recall`, `agent_context_recall_enhanced`, `agent_memory_recall` (use `agent_recall`). Annotation tools superseded by the shape `internal/hud/bridge` already writes: `agent_code_annotate`, `agent_code_annotations_get` (use `agent_context_add` with `entry_type="annotation"` + `agent_context_search`). Recipe aliases that forwarded verbatim to the engram tools: `agent_recipe_add/recall/list` (use `agent_engram_add` with `tier=1`, `agent_engram_recall`, `agent_engram_list`). Manual triggers for the always-on background `WorktreeReconciler`: `agent_worktree_cleanup`, `agent_worktree_reconcile`; plus `agent_worktree_status`, subsumed by `agent_worktree_list(include_git_status=true)`. And `agent_context_recall_since`, the delta/cursor primitive that never acquired a consumer (no CLI, no HUD endpoint, no skill).
- **agent-context: ~1.5k lines of unreachable code removed** (`pkg/agentcontext`): `hybrid_search.go`, `parallel_embed.go`, `codebase_sync.go`, and the memory export/import trio (`memory_export*.go`) plus their `Service` wiring — all had zero non-test callers. Also removed the orphaned handlers behind tools deleted in earlier SIMP-* slices: `HandleContext{Get,Delete,Share,QueryShared,Stats,LinkCodebase}`, `HandleMemory{Compress,Merge,PolicySet,Export,Import}`, `HandleCompactionTrigger`, `HandleReconcileTrigger`, and the `TemplateSvc` deprecation stubs.
- **agent-context: false claims in the empty registrars corrected.** `tools_compaction.go` and `tools_templates.go` claimed the removed capabilities were "available via CLI: `loom agent compaction/reconcile`" and "template management via CLI only" — `cmd/loom` never imports `pkg/agentcontext` and has no such subcommands. Both files are gone; the surviving `NOTE:` comments now say the capability was removed, not relocated. Same fix for `agent_context_stats` ("CLI only") and `agent_memory_export/import` ("CLI only"). `qdrant_registry.go` said "12 active collections" while wiring 16.
