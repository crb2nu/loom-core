- **Gate the `loom-mills-operator` image build on its real inputs** (`.gitlab-ci.yml`): `build:image:loom-mills-operator` now uses the same `changes:` gate on the default branch as `build:image:loom-core` and `build:image:custom-server` (Dockerfile, build script, `go.mod`/`go.sum`, `cmd/**`, `internal/**`, `pkg/**`). It was the last image job rebuilding and re-tagging on every main merge, which spent a 4-CPU BuildKit slot and rolled the Flux-managed operator deployment on unrelated changes. The feature-branch gate is widened to the same list: the old `cmd/loom-mills-operator` + `pkg/mills` pair missed the `internal/*` and other `pkg/*` packages the operator actually links.
