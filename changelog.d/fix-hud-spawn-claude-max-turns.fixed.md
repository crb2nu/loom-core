- Claude-code spawns now honor the request's `max_turns` budget on the headless CLI path and default to 100 turns instead of a hard-coded 50. Review-stage agents that launch long test suites and poll for completion in cheap one-command turns were exhausting the 50-turn cap mid-wait, dying at `error_max_turns` (~$1.5–1.9 per failed attempt) — the entire claude-code spawn error count on 2026-08-08.
