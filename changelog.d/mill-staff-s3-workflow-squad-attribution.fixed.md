Mill Staff S3: imperative (workflow-lane) starts now carry squad attribution — `startImperativeRun` routes through the same post-commit `SquadRouter` boundary as the DAG lane and emits `reconciler.squad_routed` under `subject_kind=workflow_run` (payload gains a `lane` discriminator on both lanes), closing the hole where workflow-selected items were never squad-routed and routing data silently thinned as that lane grew. The squads `in_flight` stat now counts running imperative runs alongside active pipeline runs. Squad *outcome* recording for the workflow lane stays deliberately deferred: every v1 template stops pre-merge, so recording its always-escalating terminals would poison `SuccessRate` (whose only success signal is `merged_clean`); the seam and rationale are documented on `squads.SquadRoutedWorkflowSubjectKind`.
