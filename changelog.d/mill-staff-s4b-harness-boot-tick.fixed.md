Mill Staff S4b: overseer harnesses now take a first tick ~2 minutes after boot (`Harness.BootTick`, first delay = min(BootTick, Interval)) instead of waiting a full interval. Observed on the first soak evening: three operator Recreate rollouts reset the groomer's 60-minute clock each time, so it never reached a single tick while the 5m/15m agents ticked normally. Zero BootTick preserves the old interval-first behavior.
