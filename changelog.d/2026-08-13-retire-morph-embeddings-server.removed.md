- Retired the `morph_embeddings` MCP server. Morph decommissioned its embeddings endpoint: with the API root live (401 without a key), `morph_embeddings_embed` returned `NOT_FOUND: Morph: resource not found` for both the deployed model (`embedding-001`) and `morph-embedding-v3`, so this was a model-agnostic endpoint retirement rather than a config typo. The server was still running in `loom-hub` and still advertised to every agent with `always_allow` on all three tools, so any call returned an error. Nothing consumed it — the only references were a bulk-tools registration and two stale doc rows, and pod logs showed zero tool invocations. Embedding now goes exclusively through FlexInfer via `pkg/flexinfer` defaults. Removes `cmd/mcp-morph-embeddings`, `k8s/base/servers/morph-embeddings`, the `mcp/context/registry.yaml` entry, the gateway/mcpo/mobile-hud wiring, the Makefile target and `MCP_SERVERS` entry, and the `internal/daemon/bulk_tools.go` registration. `morph_fast_apply` is a separate product line (`morph-v3-large`) and is untouched — its liveness was not tested here.
