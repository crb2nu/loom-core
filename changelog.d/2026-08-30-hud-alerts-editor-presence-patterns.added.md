- **HUD: alert-rule editing, Fleet-consolidated agent actions, Pattern Loom repairs** (`internal/hud/frontend`): the Alerts panel's rules table gains enable/disable, edit, add, and delete — every write re-reads the live set before the whole-set-replace PUT (the endpoint has no CAS), round-trips `last_fired` verbatim (it carries live cooldown state), edits durations in human units instead of raw nanoseconds, restricts condition types to the three the engine actually evaluates, and says plainly that rules live in engine memory until restart. The Presence ▸ Agents tab is retired — Fleet is the canonical roster, and its rows gain the Task/Nudge actions (with the dispatch/nudge modals) that previously lived only there; Presence keeps Claims/Worktrees/Handoffs/Diagnostics. Pattern Loom fixes from the surface audit: the Spinning Room pattern picker sent `?status=all`, which the backend matches literally — an empty catalog on every open, on all four pages that mount it (now omits the param); the Patterns page can finally stamp→enqueue to Mills (previously only the dialog could, while the page silently wrote inert draft plans — the result banner now says which happened); the panel's status filter no longer leaks through the store singleton into the Factory shelf and shift report (fetch is unfiltered, the filter is view state); the shift report fetches patterns itself so attribution survives being opened from Bolts; the dead `/api/engrams` poll leg is gone (the graph carries the tree; a third of the page's requests did nothing); the engram tree count is labeled "shown" against the server's 500-node cap; and `#engrams` now lands on the page that hosts the tree.
