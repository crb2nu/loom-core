- **Wire the HUD auto-fix engine into production init behind `HUD_AUTOFIX_ENABLED`** (`internal/hud/autofix_wiring.go`, `internal/hud/embed.go`): `autofix.NewAutoFixEngine` previously had zero production callers, so every shipped HUD served empty `/api/autofix/*` proposals/executions and 503'd on diagnose/approve/reject. The engine now initializes when the feature flag is on (default off until soak, mirroring the overseers pattern), sharing the coordinator's FlexInfer client (breaker + usage accounting) or building a dedicated one from the same config. New plumbing: `AgentBridge.GetJobTrace` (gitlab `get_job_trace`) feeds failed-job log snippets into diagnosis, and `AgentBridge.RetryPipeline` (gitlab `retry_pipeline`) replaces the retry strategy's success-faking no-op — retry now reports honest failure when no retrier or diagnosis is available. Also fixes the `agent_fix` spawn passing the project name as `base_branch`. Enable via `HUD_AUTOFIX_ENABLED=true`, `embedded_hud.autofix_enabled: true`, or `loom hud --autofix-enabled`.
