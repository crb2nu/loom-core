- **Auto-rate hardening: spawn-state prune-on-write + deadline-SIGTERM classification** (`internal/spawn/store.go`, `pkg/mills/pipeline/spawn_class.go`, `pkg/mills/store/dao_telemetry.go`): two of the top escalation buckets from the 2026-07-26 audit (73 failed stage-attempts across 30 escalated runs; auto-merge rate 17.4%). (1) A spawn-state Save that trips the ConfigMap size budget now self-heals instead of failing: terminal entries are pruned oldest-first — never a non-terminal entry, never the entry being written — down to the 80% soft limit, then the write proceeds; refusal is preserved only when nothing terminal can be pruned. Before, the guard refused outright and every dispatch between `PruneTerminalToSoftLimit` loop ticks 400'd with "persist owned spawn before dispatch" (18/73 attempts, escalating unrelated runs as class=code). (2) `agent CLI exited 143` — the spawn command's own `timeout -k 10` SIGTERM at the exec deadline — and the reconciler's "spawn deadline exceeded" backstop now classify as `spawn-agent-timeout` (retryable infra, auto-requeue eligible) instead of falling through to class=code (9/73 attempts, most of them mid cold-cache `go test` compiles that had already finished authoring the change). Telemetry roll-up gains the matching `exited 143` needle so the stage-class buckets agree with the escalation classifier.
