- **Keep long-running Council HTTP responses writable through completion** (`cmd/loom-mills-operator/handlers_council.go`, `internal/hud/domain/mills/proxy.go`): Council run and dry-run requests now extend the operator response deadline through the existing ten-minute request budget plus a short margin, and the HUD uses a matching route-specific upstream timeout while ordinary polling routes remain fail-fast. A successful run no longer surfaces as a proxy error that could invite an accidental paid retry.
