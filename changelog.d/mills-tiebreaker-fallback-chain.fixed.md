- Make Mills dissent tiebreakers fall back from Anthropic to OpenAI Responses and FlexInfer on vendor outages, preserving skipped-hop reasons and winning-vendor attribution (`pkg/mills/gates`, `pkg/mills/clients`, and `cmd/loom-mills-operator`). Configure `gates.tiebreaker.backend`, `model`, and ordered `fallbacks` in `pkg/mills/policy.go`; omitted fallbacks default to OpenAI `gpt-5.5` and the resolved local FlexInfer judge model, while `fallbacks: []` disables fallback. Adjacent vendors must differ. `LOOM_MILLS_GATE_TIEBREAKER_MODEL` overrides the first model; `off` disables the entire chain. Parse errors and refusals allow one further eligible attempt; exhaustion retains the unresolved primary failure.
