- Fail skill generation and sync when required resources, manifest reads/writes, or stale-file cleanup fail. Publish manifests with delivered-file SHA-256 hashes only after successful delivery, use the same verified path for skill and profile sync, and prune retired files only when their recorded bytes still match. Preserve modified or ambiguous legacy files with an actionable error, keep unmanaged neighbors, and defer repo mirror cleanup until home generation succeeds.
