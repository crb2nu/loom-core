- **Mills audit reviewer traffic attributed to its own usage component** (`clients.ComponentAudit` = `mills-audit`): `FlexInferAuditReviewer.Review` now tags its context, so `mills_llm_prompt_tokens_total{component="mills-audit"}` separates the post-merge/council audit pool (2026-09-14: the largest local-lane consumer, 14K prompt tokens per merge on `qwen38-27b-xtx-warm-canary`) from the untagged `mills-flexinfer` bucket it used to share with every unlabelled caller — the bucket the overseer-triage kill-test was reading.
