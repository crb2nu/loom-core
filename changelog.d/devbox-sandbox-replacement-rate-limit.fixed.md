- devbox (K8s backend): the clientset now runs with controller-grade rate limits (50 QPS / burst 100, `DEVBOX_K8S_CLIENT_QPS` / `DEVBOX_K8S_CLIENT_BURST`) instead of client-go's kubectl defaults, the pod-gone poll relaxes from 200ms to 1s after two seconds and survives transient Get errors until its deadline, and the sandbox replacement wait is 60s and configurable (`DEVBOX_K8S_POD_GONE_TIMEOUT`). Under Mills' concurrent tests stages the shared limiter had queued requests past the callers' deadlines, so "wait to replace non-running pod: wait pod gone: … client rate limiter Wait returned an error" killed 44 of 46 replacement waits on 2026-09-04..10 before the pod was observed once. The hub devbox sandbox default memory limit rises from 4Gi to 6Gi so `pkg/mills` test packages stop being killed at the limit.
