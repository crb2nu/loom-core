- Devbox sandbox and buildah build pods now derive the group-qualified repo path (`services/…`, `libs/…`) from their `/workspace` layout when callers don't thread it, and clone URLs dedup the base/path boundary segment — fixing `libs/mcp-go` clones in the Mills tests stage and staying correct under config/image rollout skew. `DEVBOX_K8S_GIT_BASE_URL` moves to the group-neutral root.
