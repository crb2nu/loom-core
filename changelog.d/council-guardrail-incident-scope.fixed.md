- **Mills council**: the editor guardrail now applies the external-incident
  follow-up contract per proposal instead of per run. A proposal that never
  references the outside system is kept (unlabeled) even when the run's
  research section classifies a workspace error cluster as an external
  dependency incident; the sidecar note reports `K repo-scoped proposals
  preserved`. Previously every proposal in fifty consecutive cron runs
  (2026-08-21 → 2026-09-02, ~$133) was dropped as "external-only" because the
  brief mentioned GitLab CI and Longhorn failures, so the council produced no
  backlog delta for twelve days.
