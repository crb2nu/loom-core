- Stamp every Mills run with a `run.provenance` event (actor `reconciler.provenance`) at the post-commit run-start boundary, keyed `pipeline_run`/`workflow_run`, carrying the policy checksum, resolved stage models, and prompt-template hashes that produced the run — the join key per-version win-rate and cost analytics need (`pkg/mills/run_provenance.go`, `pkg/mills/reconciler.go`, `pkg/mills/policy_manager.go`, `cmd/loom-mills-operator/run_provenance.go`). The stamp is exactly-once across duplicate dispatches (read-before-resolve, so crash replay after a policy hot-reload cannot rewrite it), best-effort (a failed stamp never blocks a run), and counted by `mills_run_provenance_stamps_total{lane,outcome}`; it records start-time intent only, with mid-run model swaps still attributable from `pipeline.stage.agent_routed`.
