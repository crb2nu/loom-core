- Drop ten env vars from the `loom-gateway` Deployment that the live gateway binary never read — the `FI_MCP_RATELIMIT_*` block, `FI_MCP_BACKEND_IDLE_TIMEOUT`, `FI_MCP_AUTH_MODE`, `FI_MCP_POLICY_DEFAULT`, `MCP_HUB_NAMESPACE` and `FI_MCP_LOG_LEVEL`. The image is built by `libs/fi-mcp-kit`, whose `cmd/fi-mcp-gateway` reads only `PORT`, `HUB_TOKEN`/`FI_MCP_HUB_TOKEN` plus three flags; no behaviour change (`k8s/base/servers/gateway/deployment.yaml`, evidence in `libs/fi-mcp-kit/docs/harvest-fi-mcp-gateway.md`).
