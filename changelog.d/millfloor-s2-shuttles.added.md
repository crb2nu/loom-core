- HUD Mills: new mill-floor **Shuttles** view (S2) — active pipeline runs on the
  split-flap DepartureBoard with subruns grouped under their parent, each row
  opening the shared run-detail drawer. A separated, honestly-labelled "weaver
  capacity" strip reports the agent fleet and hides itself when
  `/api/shuttle/status` errors, so a fleet outage never blocks the run board.
