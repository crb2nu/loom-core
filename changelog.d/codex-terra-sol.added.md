- **GPT-5.6 Codex models wired into Mills spawns and the workspace Codex CLI config** (`pkg/mills/policy.go`, `pkg/mills/pipeline/dispatcher.go`, `pkg/mills/clients/spawn.go`, `internal/spawn/types.go`, `internal/hud/spawn.go`, `cmd/loom-mills-operator/main.go`, `pkg/generator/configs_codex.go`, `pkg/generator/configs.go`, `pkg/sync/manager.go`, `mcp/context/registry.yaml`, `docs/MILLS.md`): the Mills operator can now pick a vendor-native LLM model per spawn-driven stage via a new `pipeline.stage_models` policy map (keys `plan_slice|implement|pr_self_review`, free-form model ids validated to a sane token shape). It is orthogonal to `stage_agents` (which picks the vendor) and resolves with the same precedence — `LOOM_MILLS_SPAWN_MODEL` env break-glass > policy entry > vendor default. The resolved model threads `SpawnRequest.AgentModel` → HUD spawn API `model` → `spawn.Request.Model` → `codex exec --model` (`resolveCodexModel` now prefers the per-request model over `SPAWN_CODEX_MODEL`/compiled default); non-codex agents log an ignored-override wiring line rather than erroring. `loom sync codex --regen` now emits `model = "gpt-5.6-terra"` as the `~/.codex/config.toml` default plus a standalone `~/.codex/sol.config.toml` planner overlay (`model = "gpt-5.6-sol"`, selected with `codex --profile sol`) — the Codex 0.134.0+ profile format, since `[profiles.<name>]` tables in `config.toml` are no longer read (per learn.chatgpt.com/docs/config-file/config-advanced). The stage→model mapping (implement → gpt-5.6-terra, plan_slice → gpt-5.6-sol) ships as a deployed-policy edit, not a compiled default.
