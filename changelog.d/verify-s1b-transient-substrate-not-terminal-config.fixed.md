- Stop escalating two substrate faults as terminal config: a git-clone init container dying on a GitLab transport blip (`error: RPC failed; HTTP 502`, `expected flush after ref listing`, `early EOF`, `remote end hung up`) now classifies as a transient git-clone transport error and retries free instead of falling through to the exit-128 "no git message was captured" default, and an MCP hub call-slot timeout in the autonomy probe (`wait for call slot: context deadline exceeded`) now counts as a transient capability so the run enters the bounded capability hold instead of tripping `capability_red` on the first failure (`transport.IsTimeout`, `pkg/mills/council`, `pkg/mills/pipeline`).
