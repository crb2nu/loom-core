- Exclude equal non-empty plan IDs and differing plan-slice title suffixes before queued and merged-canonical duplicate scoring or LLM verdicts in `pkg/mills/overseer/groomer.go`, with regression coverage in `groomer_test.go` and `groomer_merged_dedup_test.go`. Emit plan-sibling skip evidence and count dry-run skips as non-interventions. Restart the groomer's seven-day soak after merge; promotion remains gated.
