`test:integration` all-servers smoke test: gate `mcp-weaver`, `mcp-mentatlab`, `mcp-jobsearch` and `mcp-linkedin` on their required endpoint/credential env vars, gate `mcp-devbox` on a `docker` CLI being on PATH, and skip the `mcp-hub-wrapper` CLI, instead of failing with `initialize failed: read response: EOF`. The test had been silently skipping in CI (no `bin/` because the per-SHA binaries cache never restored) until the runner cache started working on 2026-09-11, at which point every merge-queue pipeline went red on these six servers.
