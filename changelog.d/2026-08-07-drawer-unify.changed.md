- Collapse the Mills pipeline and workflow run drawers onto the shared `DetailDrawer` so both inherit one focus trap, Escape/backdrop close path, and the mobile bottom-sheet treatment instead of three hand-rolled copies of the same chrome — `WorkflowRunDetail` gains the focus trap it never had, and `DetailDrawer` grows backward-compatible `titleContent`, `headerActions`, `contentPadding`, and `closeLabel` props (the last keeping each drawer's ✕ named after the surface it dismisses, e.g. "Close run detail", rather than a generic label) (`internal/hud/frontend/src/lib/components/shared/DetailDrawer.svelte`, `internal/hud/frontend/src/lib/components/mills/PipelineRunDetail.svelte`, and `internal/hud/frontend/src/lib/components/mills/WorkflowRunDetail.svelte`).
