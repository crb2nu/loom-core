- Mills council: the editor prompt's stable prefix now carries a slice-scope authoring contract — declare every directory the work touches, including the shared shells, styles, stores, DAOs, and clients a slice only reaches through, and prefer a directory you are sure of over a guessed basename. Reduces the scope-gate escalations caused by under-declared `Slices[].files` (three items escalated this way on 2026-07-26, each burning a full respawn per retry).
