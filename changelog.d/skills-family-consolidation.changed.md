Consolidated overlapping skills in `mcp/context/skills-registry.yaml`: merged `research-docs-workflow` into the on-demand `research` skill (always-on copy reduced to a short pointer), folded `agent-recipes` into `agent-engrams` (recipes are Tier-1 engrams), folded `auto-quality-gate` into `quality-gate-loop` (with `devbox_quality_gate` as the preferred entrypoint), and added scope boundaries across the memory cluster (`agent-context`, `multi-agent-handoff`, `long-term-memory`, `memory-practices`). The auto-ship tail (commit → push → MR → auto-merge → CI poll → cleanup) repeated across ~12 loop skills is now a compact stanza pointing at the canonical copy in `mcp/skills/_shared/landing-procedure.md`; `commit` and `ci-green-merge-loop` keep the full detail.
