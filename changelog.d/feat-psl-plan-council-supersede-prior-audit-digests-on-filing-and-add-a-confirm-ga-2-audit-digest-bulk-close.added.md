- Supersede the previous open audit digest when a new daily digest is filed (`pkg/mills/audit/digest.go`, `pkg/mills/audit/followup.go`): the newest still-open prior `audit-digest` issue gets a retry-safe `superseded by #<new>` note and is closed, so the open pile stops growing by one issue per day. GitLab support lands as `FindPreviousOpenAuditDigest`/`IssueHasComment` in `pkg/mills/clients/gitlab.go` with a compile-time capability guard in the operator, and `mcp/skills/mills-ops/SKILL.md` gains a runbook entry for draining the existing pile with the confirm-gated `loom audit-advisory-sweep` (which already supersedes the bulk-close helper this item first proposed).
