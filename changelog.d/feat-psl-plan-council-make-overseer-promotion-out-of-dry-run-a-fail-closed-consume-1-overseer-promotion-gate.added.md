- Add `overseer.PromotionGate`, a fail-closed consumer of the S2 soak-complete artifact: it reads the shift report's `generated_at` + `soak_progress` projection and returns active mode only when `SoakProgress.CompleteAt` accepts it, staying in dry-run with a logged reason for missing, unreadable, malformed, future-dated, short, or diverged evidence. Contract in `docs/mill-staff-s2-promotion.md`.
