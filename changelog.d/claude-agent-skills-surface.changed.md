Migrated the Claude skills target from legacy `.claude/commands/<name>.md` slash commands to Agent Skills bundles at `~/.claude/skills/<name>/SKILL.md`: all 68 command-type skills now ship as bundles with their scripts/references/assets included (34 skills previously lost their resources on Claude), stale generated command files are pruned name-scoped, and the registry gains `when_to_use` (trigger phrases, 1,536-char combined cap validated), `disable_model_invocation` (user-only ship loops: commit, caveman-commit, ci-green-merge-loop), and `context` frontmatter pass-through. Legacy `type: command` generation still works during transition. Kill-tested live: generator-produced bundle listed and loaded in a running Claude Code session. Spec: `.loom/product-spec-claude-agent-skills-surface-2026-07-19.md`.
