- Retired the unbuilt atomic multi-repo merge capability (Mills v2 slices 4.2–4.5): removed `crossrepo.Integrator`/`Planner`/`Registry`, the pipeline runner's `CrossRepoIntegrator` seam, `CrossRepoDAO`, the `/api/mills/cross-repo/runs` REST endpoints, the `loom mills cross-repo` CLI, and the HUD Cross-Repo panel. No production code ever wrote a `cross_repo_runs` row, so those read surfaces were permanently empty; migration 028 drops the table. Per-item cross-repo *execution* via `BacklogItem.TargetProject` is unaffected and remains live, as does the `cross_repo.*` policy gate. `QuiescenceCounts` no longer reports `active_cross_repo_runs`.
