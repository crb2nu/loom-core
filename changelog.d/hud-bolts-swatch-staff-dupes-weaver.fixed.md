- **HUD: Bolts drops its swatch column, Mill Staff stops stacking its departments twice, Factory lanes always name a weaver** (`internal/hud/frontend/src/lib/components/mills/{BoltsPanel,MillStaffPanel,ShuttleBoard}.svelte`, `lib/utils/shuttleBoardHelpers.ts`, `lib/panelRegistry.ts`): the Bolts table's "swatch" column was a seeded pattern keyed on the run id that encoded nothing about the bolt; it is gone. The Mill Staff tab used to stack the Drawing Office, Drawing-in, and Alley panels beneath the staff overview even though each is its own tab beside it; the tab is now the overview alone (department tiles plus the evidence reports) and `MillStaffGroup` is removed. On the Factory shuttle board the weaver column read "—" for any pick still in flight, because stage records are opened with empty `Model`/`Backend` and filled only at pick end, and for research picks that run on flexinfer without a routing block. The lane now falls back, in order, to a `weaver-<model>` spawn id, the run's provenance `stage_models` for that stage (marked *routed*), or a bare spawn (marked *model pending*), and names the operator for the picks no model runs (tests, mr, ci_watch, merge, cleanup — marked *no model*); it never borrows the previous pick's model.
