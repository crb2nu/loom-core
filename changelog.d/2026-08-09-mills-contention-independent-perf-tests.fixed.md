- **Make the Mills queue-depth gates measure the code instead of the runner** (`pkg/mills/reconciler_test.go`, `pkg/mills/store/dao_pipeline_start_test.go`, `pkg/mills/store/dao_backlog_admission_plan_test.go`): two timing-bound tests reddened `test:race` on main whenever concurrent main pipelines shared a runner (pipelines 22848/22888/22898, 2026-08-09). Both absolute wall-clock bounds are replaced by contention-independent measurement. `ClaimPipelineStart`'s 10k-queue gate now samples a shallow and a deep store in lockstep and bounds the p50/p95 *ratio*, so CPU contention cancels instead of accumulating; the reconciler tick's `latency < 2s` is replaced by an `EXPLAIN QUERY PLAN` assertion that the admission read stays index-ordered and never sorts the queue before its `LIMIT`. Statement-count bounds are unchanged. The new gates are strictly more sensitive than the ones they replace: with `idx_backlog_state` dropped, the ratio gate fires at 4.85x where the old 100ms bound still passed.
