The fleet-reliability gate supports operator-approved, self-expiring
benchmark waivers (`waivers` in the suite manifest): per-benchmark
time-regression caps with a mandatory reason and an inclusive expiry date,
recorded in the benchmark report whenever applied. Deliberate performance
trades now merge auditable instead of blocked-or-silent.
