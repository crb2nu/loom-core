- **Keep the poll history on every `PollPipeline` error return** (`pkg/mills/clients/gitlab.go`): the MR-fetch, branch-mismatch, and branch-pipeline-lookup paths all returned a zero-valued response, so `ci_watch` appended an empty tail and the escalation lost the very poll history that explains the failure. All non-timeout error returns now carry the accumulated log tail plus the last observed pipeline URL and status.
