- Mills model-economics telemetry no longer folds deterministic stage rows (tests, ci_watch, mr, merge, cleanup, adopted-branch implement — no model, no backend, no cost) into an `unknown`/`unknown` model tier. Live data showed 27 such rows with 9 errors rendering as a 33%-error-rate "unknown model"; rows that carry cost without identity still bucket under `unknown` so spend never disappears.
