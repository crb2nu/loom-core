- HUD frontend coherence sweep: landing surfaces (Operator Deck, Overview, merge queue, RBAC/OTel cards) now surface fetch failures instead of reporting a false all-clear; workflow approvals, the Mills kill switch, and bulk destructive actions gained ConfirmDialog gating with busy/acknowledge feedback; SSE-triggered refetches are coalesced and fleet joins memoized to stop polling amplification; one status→color language (booting spawns no longer render as faults) and one relative-time formatter across all panels; `color-scheme: dark` so native dropdowns stop flashing white; app shortcuts are suppressed while dialogs are open, log viewports and the command palette are keyboard-operable, and a stale lazy panel chunk now offers reload; Presence tabs unified on the shared visual language. Removed three dead modules (widgets/ErrorBanner, mills/MillsPanel, the components/shared barrel).
