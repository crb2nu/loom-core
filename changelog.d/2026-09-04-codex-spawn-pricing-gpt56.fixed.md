- **Codex spawns are no longer recorded at $0** (`internal/hud/bridge/spawn_pricing.go`, `internal/hud/spawn_codex_parser.go`, `pkg/llmpricing`, `pkg/mills/clients/council_openai.go`): the spawn price table stopped at gpt-5 while production spawns run gpt-5.6-terra / gpt-5.6-sol / gpt-5.5, so `EstimateCodexCost` returned 0 for every real model and Mills stage records and budget accounting saw each codex-run stage as free. The HUD spawn parser and the Mills council now share one OpenAI price snapshot in `pkg/llmpricing` (verified 2026-09-04 against the public price list; gpt-5.6-* rates in the council table were stale and are corrected), an unknown model is billed at the `gpt-5.5` default rate with a warning instead of $0 so a model newer than the snapshot over-estimates rather than under-reports against a budget cap, and the bridge default now matches the model the spawn server actually pins.
