- **Reconciled Mills workflow-turn-liveness (loom-core#300) status** (`.loom/188-reconciliation-workflow-turn-liveness-2026-07-17.md`, `docs/runbooks/mills-workflow-s1c-killtest.md`): recorded, with `file:line` + commit + green test evidence, that the #300 orphaned-turn recovery is already merged and verified — recover-before-reconcile boot ordering (`internal/hud/embed.go`), keyed re-drive / unkeyed fail-fast (`internal/hud/spawn.go` `recoverInterruptedSpawns`/`classifyInterruptedSpawn`), the reconciler restart-durable deadline backstop (`internal/spawn/controller.go` `spawnDeadlineExceeded`), and terminal GC (`reapTerminalSpawn` + prune loop). The deployed S1c gate stays closed pending the codex-led S4 pod-owned execution supervisor/reaper (`codex/arch-fleet-s4-durable-execution`), which the runbook's process-continuity contract requires; the current re-drive path is expected to fail that contract, so the live 3-run canary must wait for S4. No runtime behavior change in this entry — documentation/handoff only.
