- **Codex `approval_policy` flips `"never"` → `"on-request"`** (`mcp/context/registry.yaml`, `platform_permissions.codex.settings`; regen with `loom sync codex --regen`, mirror with `loom sync mirror`). Under `"never"` the model is told sandbox escalation is impossible, so interactive/desktop Codex sessions never even attempt `git commit`/`push` against the workspace-write sandbox's by-design read-only `.git` (openai/codex#15505) and autonomous shipping wedges at commit with ".git is read-only in this environment" reports — observed 2026-08-30 on the flexinfer-site editorial-refresh thread, where the session transcript contains zero git write attempts. The 2026-07-25 execpolicy mitigation cannot unwedge the desktop surface on its own: the app's unified exec wraps every command as `/bin/zsh -lc '<script>'`, which the `["git"]` prefix rule cannot match (`codex execpolicy check -- git commit` → allow, but `-- /bin/zsh -lc 'git commit …'` → no match, verified on 0.144.6 and the app-bundled 0.151.0-alpha.7.1). `"on-request"` restores the escalation prompt (approve once → Codex persists an allow-rule) while execpolicy-matched commands keep running prompt-free; headless Mills spawns are unaffected because they pass `codex exec --dangerously-bypass-approvals-and-sandbox` explicitly. Rules-block comment and justification text updated to match the new posture.
