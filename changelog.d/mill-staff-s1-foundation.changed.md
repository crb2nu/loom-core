Mill Staff S1 foundation cleanup: new `pkg/mills/textsim` leaf package is now the single title-similarity implementation (tokenizer, Jaccard, gray-band floor) shared by council dedup, the overseer groomer, and overseer policy validation — replacing three mirrored copies; direct cross-lane imports between council/squads/overseer are now zero. Squads shed dead code (Planner, FlexInferSpawner, MarkRegressed/UpdateOutcome and the never-written `merged_regressed` outcome, ~1.4k LOC) and `policy.squads.routing.min_confidence` is now actually honored by the router (new `squads.RoutingPolicy` seam, hot-reloadable); manifest fields `tests/gates/ensemble/budget_share/recursion_enabled` are explicitly documented as reserved/unenforced. Stale slice-era comments corrected across overseer policy, workflow scheduler/interp, and the operator.
