iOS Operator deck — command the fleet in place. Tapping an agent row now opens a command sheet with direct controls (end session, and message / interrupt / stop for k8s spawns) instead of bouncing to the Agents tab; the action set is derived from live agent state so the sheet never offers a control the HUD would refuse. A new "Needs you" section surfaces everything blocked on the operator — workflow approvals, agent handoffs, and auto-fix proposals — with inline approve/accept/reject. Deck roster rows gain orphan/attention/k8s badges, include flagged offline agents, and hold a stable conversation-grouped order across polls.
