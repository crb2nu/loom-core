- **Resolved model-wiring endpoint for the HUD Mills Overview** (`cmd/loom-mills-operator/handlers_wiring.go` `GET /api/mills/wiring`, `main.go` `buildWiringSnapshot` wiring capture, `internal/hud/domain/mills/mills.go` proxy route): the HUD Mills Overview hid the operationally-critical routing config — which models/backends the rubric judge, research/weaver, council contradiction judge + reviewer lenses, and each spawn stage actually use — even though the operator already resolves and logs all of it at startup. A new read-only endpoint now serves a `WiringSnapshot` captured once at wiring time, ADJACENT to the existing startup wiring log lines so it can never drift from what the operator logged: judge/weaver backend + effective model + resolved fallback chain + `registry_fallbacks_disabled`, council editor/judge/lens backends (a lens degraded to the deterministic fake fallback surfaces backend `"fake"` honestly), per-stage spawn agent/model with `source` precedence attribution (`env` > `policy` > `default`), spawn env-override flags, LLM-gate enablement + dissent tiebreaker, LiteLLM-configured flag, and autonomy-enabled + a cheap policy checksum. Values are the RESOLVED effective config (post env-precedence, post policy), never raw env echoes; every array encodes `[]` never null and no secret, key, or credentialed URL is ever emitted. Open read (no auth) proxied through the HUD like `/api/mills/status` and `/api/mills/telemetry/stages`; static (the routing changes only on restart). New `FlexInferClient` getters (`JudgeModelFallbacks`/`WeaverModelFallbacks`/`RegistryFallbacksDisabled`) and exported `JudgeMaxTokensFromEnv` expose the effective judge/weaver resolution without re-deriving it. Pinned by a zero-value contract-shape test (never-null arrays, exact snake_case keys), env-precedence population tests (litellm-configured vs default-flexinfer vs misconfigured-loud-fallback → EFFECTIVE outcome), council-lens backend mirroring, stage source attribution, and a HUD proxy pass-through test.
