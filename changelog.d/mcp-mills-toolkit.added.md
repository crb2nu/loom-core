- `mcp-mills`: MCP toolkit for operating the Mills factory — status/KPIs, backlog list/get/post, pipeline run queries, one-shot escalation diagnosis with a requeue-vs-rescue verdict, and revision-safe state transitions that refuse to requeue over an existing implement branch (the collision that produces FailureClass=configuration escalations). Reads are tokenless; mutations require `LOOM_MILLS_TOKEN`. (`cmd/mcp-mills`)
