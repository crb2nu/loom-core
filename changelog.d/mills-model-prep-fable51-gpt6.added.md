- **Mills: prep for new model tiers (Claude Fable 5.1, GPT-6 "astra") — policy-priced models, priced Fable 5.1, honest codex spawn costs, SDK driver `--model`** (`pkg/mills/clients/model_prices.go`, `pkg/mills/policy.go`, `cmd/loom-mills-operator/model_prices.go`, `internal/hud/bridge/spawn_pricing.go`, `internal/hud/spawn_sdk_driver.go`, `tools/spawn-driver`): a new `budgets.model_prices` policy block prices a model the compiled tables do not know — keyed by the exact id the policy routes, with a provider (`anthropic` | `openai` | `openrouter`), hot-reloaded and winning over the compiled row — so a tier can be priced the day it ships instead of on the next image. That matters because council/spin lookups are exact-match and an unpriced model does not report $0: it loses the per-attempt ceiling and charges the whole run reservation on a timeout. `claude-fable-5-1` gets a provisional compiled row (Fable 5 rates; confirm and correct). The codex spawn estimate table learns the gpt-5.6 family (sol/terra/luna) and gpt-5.4; every gpt-5.6 spawn had been estimated at the gpt-5-codex fallback rate, a 4× undercount for sol. The SDK spawn driver (`loom-spawn-driver`) now accepts `--model` and passes it to the Claude Agent SDK / Codex SDK options; the Go orchestrator forwards the request's routed model on that path as it already did on the CLI path, so a `stage_models` / `agent_routing` pin can no longer be silently dropped. `docs/MILLS_RUNBOOK.md` gains a "Rolling out a new model" section; the stale `docs/MILLS.md` ensemble example is corrected to backends that exist.
