S6-full merging canary: the Layer-3 workflow runtime gains a journaled merge() effect (7th confined builtin) driven by an end-to-end idempotent operator merge executor (lookup-first branch/commit/MR ensure + the pipeline lane's merged-state-reconciling GitLabClient.Merge); the S1c kill-test gains a -merging mode (template v3) that evaluates PASS-3 (no double-merge across crashes) from real GitLab plus journal evidence instead of deferring it.
