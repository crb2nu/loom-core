Bump `google.golang.org/grpc` v1.82.0 → v1.82.1, patching GO-2026-6061
(xDS RBAC authorization engine and HTTP/2 transport server). The advisory is
**reachable** from our code — govulncheck reports 11 symbol traces through
`pkg/mills/workflow/killtest/spawn_watch.go` and `cmd/mcp-gcp/main.go` — so
symbol-mode scanning failed the build, which in turn blocked every image
build behind the deploy stage. Post-bump scan: "No vulnerabilities found."
