- **HUD: imperative-lane surfaces** (S7 frontend slice). The Workflows table and run drawer now render the S7 frozen identity: `template@version` in the list, and interpreter version pin, deterministic `mills-wf/<run-id>` work-product branch (with copy), and the pretty-printed frozen selection params (content hash + clamped params + validated enums, served detail-only by a new `workflow_params` field on `GET /api/mills/workflow/runs/{id}`) in the drawer. The BacklogDetail drawer closes the imperative half of "why is this escalated?": items routed through `ClaimWorkflowStart` have no pipeline run, so the drawer now lazily loads workflow runs, shows an "Imperative workflow" section (the item's template selection + per-run cross-links into the Runs subview), and the escalation attention banner derives the terminal-settle context from the run row itself — outcome, work-product branch with copy, and the "pre-merge template — nothing was merged" reminder — so the reviewer lands with everything the settle recorded.
