Mills merge queue: the tree proof from !1910 failed closed on every head because this GitLab's single-commit API returns no `tree_id`, so no candidate could merge after the rollout (!1918 sat at the head for five hours with a green pipeline). Tree equality is now proven with the compare API (empty straight diff), memoized per project, and an exact-SHA pipeline needs no tree evidence at all. Speculation no longer tries to stack an MR on top of its own second queue entry (720 failed cherry-picks in three hours), remembers a failed base instead of retrying it every tick, and the external enqueue refuses a second candidate for an MR the queue already holds (`outcome: active`).
