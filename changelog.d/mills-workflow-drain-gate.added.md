- **mills: workflow interpreter drain gate — the S6-full CI deploy-safety gate** (`pkg/mills/workflow/surface_test.go`, `scripts/ci/check_workflow_drain_gate.sh`). The imperative runtime's replay-compatibility surface (universe builtins, canonical-args encoding, step-key scheme, idempotency-key derivation, host ceilings, engine version) is now rendered into `testdata/interp_surface.golden` by `TestInterpreterSurfaceGolden`; any change forces a golden update, and CI fails an MR whose diff touches the golden or the `go.starlark.net` requirement unless the final commit carries `[workflow-drain-confirmed]` — the author's assertion that in-flight imperative runs were drained before deploy. `TestInterpreterVersionMatchesGoMod` binds `HostInterpreterVersion` to the go.mod engine version, and caught a real drift on first run: the engine was `v0.0.0-20260708150628` since the S0 port while the pin constant still said `v0.0.0-20260522144826`, so the version-drift refusal could never have fired across an actual engine change. Constant corrected (no in-flight imperative runs existed to strand). Closes the last non-deployed S6-full deliverable from `.loom/134`.
