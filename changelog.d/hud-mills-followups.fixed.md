- **HUD Mills nav badges, mobile table captions, Audit column names, Eval empty filter** (`internal/hud/frontend/src/App.svelte`, `lib/stores/mills.svelte.ts`, `lib/components/shared/DataTable.svelte`, `lib/components/mills/{AuditPanel,EvalPanel,MillStaffGroup}.svelte`): entering the Mills view now primes the shared Mills store once per session, so landing on Audit, Policy, Patterns, Drawing-in, or The Alley no longer shows Warps/Shuttles/Sparks/Bolts/Council/Eval/Policy badges as 0 until a polling tab is visited. DataTable's ≤800px stacked-card mode now stamps each cell with its column label (the comment promised captions the CSS never produced), so phone cards are no longer bare values. The Audit list gets a column-name strip over its six-value rows, sharing one grid with the rows so the names cannot drift. Eval's loop filter shows "No Loop X scores in this window" with a reset link instead of a header over an empty body. Mill Staff drops the deprecated `svelte:component`.
