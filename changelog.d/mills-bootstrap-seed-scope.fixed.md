- **Declare the seeded scaffold in a bootstrapped item's scope** (`pkg/mills/reconciler.go`, `pkg/mills/bootstrap/seed.go`): the plan→repo bootstrap seeds `README.md`, `.gitlab-ci.yml`, `.gitignore`, and `AGENTS.md` — and the seeded `AGENTS.md` tells the first implementer to replace the placeholder CI with real fmt/lint/test stages. The item's slices were authored before the repo existed, so they cannot name a repo-root file, and the scope amendment correctly refuses a root reach (no shared ancestor with any declared directory). The run therefore escalated for doing exactly what the scaffold asked (`services/housemd`, `1 file(s) outside slice scope: .gitlab-ci.yml`, twice on an identical diff). The mint now appends a `repo-scaffold` slice naming what it just seeded, so the ordinary scope gate passes for those four paths on exactly the run that created them; established repos keep an unchanged envelope. The path list travels on the `RepoEnsurer` interface so it can only come from the component that writes the files.
