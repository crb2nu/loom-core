**iOS companion: taste grading + coverage gate + merge queue (mobile catch-up)** (`apps/loom-companion-ios/Sources/LoomCompanionKit/{Networking/Endpoint.swift,Mills/{MillsTasteModels.swift,MillsAPI.swift,MillsControlAPI.swift,MillsShiftModels.swift}}`, `apps/loom-companion-ios/Sources/LoomCompanion/Views/Mills/{MillsScreen.swift,ShiftReportSheet.swift}`, `internal/hud/domain/mills/mills.go`): the taste epic's human loop reaches the phone. The shift report's bolts each carry the one-tap keep/meh/regret grade (`POST /api/mills/pipeline/runs/{id}/grade`, admin-gated like spin/escalate; already-graded bolts render their grade from the backlog read instead of re-offering the tap), and the Mills screen gains a **Taste — soak gate** card rendering the rolling-14d grade-coverage ratio against the ≥60% S5/S6 autonomy threshold with distance-to-gate copy, plus a **Merge queue** card (depth, waiting candidates) from `GET /api/mills/merge-queue`. The HUD's mills proxy also gains the missing `GET /api/mills/taste/aggregates` registration — the operator endpoint shipped in the taste epic's S3 but was never proxied, so no HUD client could read it (pinned by `TestTasteAggregatesRoute_ProxiesWithoutAdminGate`). All new reads follow the established degradation contract: an operator or HUD predating the routes renders no section, never an error.
