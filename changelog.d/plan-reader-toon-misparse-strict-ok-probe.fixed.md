- Stop plan-store reads (`agent_plan_list` / `agent_plan_slice_list` / `agent_plan_slice_get` / `agent_plan_get` / `agent_pattern_list`) from silently decoding colon-bearing plain-text tool errors into zero-value envelopes: `decodeListBody` now requires the envelope's boolean `ok` field (mirroring the devbox `passed` probe), callers surface the real `CallTool` error instead of dropping it, and `ok:false` refusals error instead of reading as "plan has no slices".
