Mills council brief no longer times out listing persisted incidents. `IncidentDAO.ListSince` was pinned to the time-only `idx_events_occurred` index, so the planner walked the whole 24h event window filtering for the rare incident kind and blew the 5s hot-read budget on 8 of the 16 scheduled council runs between 2026-09-03 and 2026-09-07 (`council: list persisted incidents: incident list: context deadline exceeded`). The pin is dropped so the planner takes migration 029's `(kind, occurred_at)` index; the query-plan test now asserts the unpinned plan.
