- `loom proxy`: every LLM vendor hint now shapes tools/list to the `llm-core` profile (claude-desktop, gemini, gemini-cli, zed, kilocode, kilo, cursor, windsurf, opencode join codex/claude/claude-code); those clients had been receiving the daemon's whole surface (~500 tools after the daemon ceiling, ~260KB of schema per turn) while codex sessions got the 167-tool core. `--tool-profile full` is the explicit opt-out. The proxy also reports required tools the cap displaced instead of evicting them silently.
