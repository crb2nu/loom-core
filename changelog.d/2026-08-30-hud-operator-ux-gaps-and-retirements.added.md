- **HUD operator UX: close functional gaps, retire superseded panels** (`internal/hud/domain/mills/mills.go`, `internal/hud/frontend`): the run drawer gains a **Resume** action for paused runs (pause was one-way in the HUD — un-parking took an admin curl against the long-existing `/resume` endpoint), and a scope-deferred requeue (`decision:"deferred"`) now reads as what it is — "Requeued — start held by the scheduler: …" — instead of "Can't requeue"/"Start failed", in both the run drawer banner and the backlog drawer toast. The Mills proxy allowlist gains the entries live code was already fetching through it — `GET /backlog/{id}/events` (the drawer's journey strip has been reading the SPA fallback in production) and `GET /fleet-gate/waivers` (dead Telemetry section) — plus forward registrations for `GET /bolts`, `GET /shift-report`, and `GET /taste/calibration` so the queued Cloth Hall S3/S4 frontend slices don't 404 at the HUD (neither slice owns the proxy file). The **Dispatch** and **Lifecycle** Operations tabs are retired: every Dispatch section had a newer home (Deck dispatch actions, Mills ▸ Shuttles recommendations, Overview merge queue) and Lifecycle owned no data of its own; old hashes redirect (`#dispatch` → Deck, `#lifecycle` → Timeline) and Overview/inbox attention cards now deep-link to the real surfaces (Presence for claim conflicts, Tasks for blocked work, Shuttles for routing suggestions).
