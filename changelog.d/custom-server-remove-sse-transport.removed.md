- `custom-server` (every hub MCP server pod): the legacy MCP HTTP+SSE transport is gone — `GET /sse` and `POST /messages` now answer 404 with a body naming the replacements. Streamable HTTP (`POST /mcp`) and WebSocket (`/ws`) are the only transports. Removed after all 22 mcpo consumers ran Streamable HTTP and a 45-hour soak (2026-08-29 → 08-31) showed zero `/sse` traffic on the hub; the ingress hosts `mcp-*.flexinfer.ai` keep routing, so a straggler sees the explanatory 404 (backlog `bl-mcpo-sse-removal-endgame-20260829`, `.loom/197` §4).
