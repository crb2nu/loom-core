- Merged-work semantic grounding embeds through a configurable model (`FLEXINFER_EMBED_MODEL`) and logs the resolved embed model at startup. The hardcoded `BAAI/bge-large-en-v1.5` default is not a deployed FlexInfer serving name, so the proxy rejected every embedding call and the semantic band silently degraded to lexical-only Jaccard since it shipped — the scorer's total fallback left no trace. (`cmd/loom-mills-operator`)
