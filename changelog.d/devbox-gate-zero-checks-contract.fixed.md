- Stop the Mills tests stage fabricating a "0/0 checks marked failed; gate reported not passed" verdict (escalation #322): the devbox quality-gate client now strict-parses the tool body (a boolean `passed` field is required), so a plain-text infra error like "sandbox image still building after 8m0s" propagates as an infra-classed error instead of TOON-decoding into a zero-value not-passed result that burned code-class attempts; `devbox_quality_gate` itself now refuses to report a verdict when zero checks executed (previously a silent `passed=true`), and that refusal classifies as config (terminal) rather than code.
