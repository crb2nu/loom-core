- Consolidated the FlexInfer embedding defaults (proxy URL and the `embeddings-1536` alias) into `pkg/flexinfer/defaults.go` as exported constants (`DefaultProxyURL`, `DefaultEmbedBaseURL`, `DefaultEmbedModel`, `DefaultEmbedProvider`, plus `RetiredMorph*` for the normalize paths). The same two strings had been copy-pasted into `pkg/pm/config.go`, `pkg/pm/embed.go`, `pkg/agentcontext/config.go` (default chain and Morph-retirement normalize block), `pkg/agentcontext/service.go`, `pkg/codebase/service.go`, and `pkg/codebase/config.go`; during the 2026-08-13 Morph retirement two rounds of fixes (!1584, !1589) each repaired only the copies they found. Env-chain precedence at every call site is unchanged — the constants are passed only as the terminal fallback. A new grep-based guard (`TestNoHardcodedFlexInferProxyURL`) walks the repo and fails the build if any non-test Go file spells the proxy FQDN again. `pkg/mills/clients` and `internal/hud/coordinator` intentionally keep an empty default (it disables the LLM-judged Mills gates and `coordinator.Config.Enabled()` off-cluster) and now name the constant in their docs instead of repeating the FQDN.
