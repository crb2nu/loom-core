- iOS companion: the Mills screen gains an Operator card read from `GET /api/mills/status` (a route the HUD already proxied but the app never consumed): a ready/paused/held/blocked verdict with blocker reasons, health-gate admission with its mode, a capability rollup naming any non-green row, rolling-24h budget bars per tier with a near-cap warning, the council-yield dry-spell signal, build sha, and last merge/council ages. `MillsAPIProtocol.operatorStatus()` degrades to nil (card unrendered) on an absent or unreachable operator and is defaulted so existing conformances compile. Also restores the Kit test target, whose mock had fallen behind the taste/merge-queue endpoints and no longer compiled.
