- **spawn/mills: credential-less codex spawns fail fast and classify precisely instead of burning $0 attempts as `spawn-stdin-misconfig`** (`internal/hud/spawn.go`, `pkg/mills/pipeline/{spawn_class,error_class}.go`): escalation #368 (2026-07-22, `PIPE-mills-spawn-stall-fast-kill-…`) burned implement attempts 3+4 at $0.00 in ~90s: fresh spawn pods created inside the 01:54Z fleet-rollout window ran codex against a dangling `~/.codex/auth.json` (the `cluster-agent-auth`/`codex-auth-json` secret volume is k8s-Optional, so an unpopulated mount starts the pod credential-less), and codex 0.144 still starts a thread+turn before dying on `401 Unauthorized: Missing bearer or basic authentication in header`. Because codex prints `Reading additional input from stdin...` on **every** headless exec, the stdin-misconfig classifier needle swallowed the real diagnostic — the same catch-all that mislabeled #351's version-gate 400. The same run's attempt 2 (`agent turn driver lost across mobile-hud restart`) mis-classed as `code`. Live probe evidence: the identical secret+image+argv completed a turn before (01:43Z) and after (post-incident in-cluster probe) the window, so this is a transient auth-delivery window, not token expiry; prompt delivery (stdin/file/arg, carryForwardDiff payload size) was ruled out — failing attempts reached `turn.started`. Three-part fix: **(1)** `codexAuthPreflight` runs in-pod between the session-end trap and `codex exec` (direct exec path AND inside the S4 supervisor's `hold.sh`, so every retry re-checks the mount): `[ -s ~/.codex/auth.json ]` (follows the symlink — dangling/missing/empty all trip) with the documented `$OPENAI_API_KEY` fallback honored, exiting 78 with a contract message before any unauthenticated turn; **(2)** two new spawn-infra reasons checked BEFORE the stdin catch-all — `spawn-auth-missing` (preflight message, or `401 unauthorized` + `authentication in header`, matching both raw and log-redacted forms; a credentialed-but-rejected `Incorrect API key` 401 deliberately falls through) and `spawn-driver-lost` (`turn driver lost`) — both retryable `ClassInfra`; **(3)** `retryBackoff` gives `spawn-auth-missing` a rollout-window schedule (`authOutageBackoff`: 1m·2ⁿ⁻¹ capped 4m) so a run's bounded attempts span ~7 minutes instead of re-hitting the same broken window seconds apart. Tests: `spawn_class_test.go` (real #368 producer bytes; auth-beats-stdin precedence; redacted + preflight + driver-lost forms; `Incorrect API key` non-match), `error_class_test.go` (`TestRetryBackoff_SpawnAuthMissingUsesRolloutWindowSchedule`), `internal/hud/spawn_test.go` (`TestCodexAuthPreflight_ShellBehavior` executes the guard through real `sh` against missing/dangling/empty/populated/env-fallback credential states; codex argv re-pinned with the guard).
