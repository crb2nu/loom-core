- **Merge-sort mobile attention lanes by severity under one global cap** (`internal/hud/domain/mobile/handler_dashboard.go`, `internal/hud/domain/mobile/helpers.go`): the dashboard previously appended mrwatch MR lanes after coordination lanes with independent per-source caps, so payloads could carry 16 lanes and a critical MR conflict always sorted below every info-level coordination lane — it could never claim the iOS hero card (anchored on the first lane). Both sources now merge into a single stable severity-sorted list (critical > warning > info, within-severity order preserved) with one global cap of 8 applied after the sort, so the cap sheds the least-urgent lanes.
