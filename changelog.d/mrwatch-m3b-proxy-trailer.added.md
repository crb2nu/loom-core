- loom proxy: append an MR-status trailer (`[loom] MR !NNN (<branch>): <state> — <action>`) to `git__git_push`/`git_status`/`git_commit` and `gitlab__*` merge-request tool results, sourced from the HUD mrwatch registry (`GET /api/agent/mr-status`). Delta-gated per proxy process (unhealthy states always shown; healthy states shown once on transition) and fail-open with ~30s branch/HUD caches and a 2s HUD timeout (mrwatch M3b).
