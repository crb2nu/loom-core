- The authoritative scope-overlap check (`store.BacklogScopesOverlap`, used by reconciler preflight and in-transaction admission) now excludes `changelog.d` from the directory envelope, completing the fix started in `pkg/mills/scope_overlap.go` — that first patch only touched the preflight empty-check copy, so queued admissions kept deferring with witness `changelog.d` against any running item. Identical literal fragment paths still collide.
