**devbox go-1.26 base image: golangci-lint v1.64.8 → v2.8.0** (`internal/devbox/baseimage/dockerfiles/go-1.26.Dockerfile`): the repo's `.golangci.yml` is v2-format and CI pins v2.8.0, but the sandbox base image shipped a v1 binary — so the immune-S2 CI-parity lint vaccine's first live runs all failed with "configuration file for golangci-lint v2 with golangci-lint v1" (5 items escalated overnight 2026-08-15). The published `mcp/devbox-base/go:1.26` image was rebuilt with v2.8.0 operationally the same morning; this aligns the source Dockerfile so the next `scripts/build-base-images.sh` run doesn't regress it.
