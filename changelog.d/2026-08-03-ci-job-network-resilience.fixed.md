- CI jobs survive transient LAN/HTTP2 blips instead of failing as `script_failure`: the shared `.clone_repo` source-fetch now retries HTTP/2 → HTTP/1.1 → the in-cluster GitLab service with 2s/8s backoff and `GIT_HTTP_LOW_SPEED_LIMIT`/`TIME` stall detection, and prints `CI-INFRA-FAILURE: source-fetch` as its last line when all three routes fail so triage can tell infrastructure from code. Package and image fetches now route through the in-cluster caches (`GOPROXY` → athens, images → Harbor's `dockerhub-cache`, apt → apt-cacher-ng); `lint:ci-guards` blocks regressions.
