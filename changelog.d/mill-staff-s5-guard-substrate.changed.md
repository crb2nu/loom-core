Mill Staff S5: the guarded-auto-act harness + audit recorder moved from `pkg/mills/overseer` to the new leaf `pkg/mills/guard` (overseer re-exports aliases, so its API surface is unchanged), extracted now that the council is the second consumer. The council `BacklogMutator` adopts the recorder under actor `council.mutator`: item creates audit as recorded actions, and hard/gray-band/plan-lane dedup skips audit as observations — the unified Mill Staff audit trail alongside `overseer.*`. Non-overseer actors label `mills_overseer_actions_total` under their full actor string.
