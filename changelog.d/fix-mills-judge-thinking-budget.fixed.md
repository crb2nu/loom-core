- The Anthropic gate-dissent tiebreaker (rubric judge) now disables extended thinking on its requests. Adaptive thinking shared the judge's small `max_tokens` budget and, on complex diffs, claude-sonnet-5 could think through the entire budget and return an empty text body — surfacing as `tiebreaker unavailable ... unparseable response; raw=""`, which left near-miss primary fails unresolved and escalated runs that a working tiebreaker would have settled. The council editor keeps adaptive thinking (claude-fable-5 rejects a disabled config).
