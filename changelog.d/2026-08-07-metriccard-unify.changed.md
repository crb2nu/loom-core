- Convert the five Mill Staff evidence tiles (promotion, judge calibration, regressions, config outcomes, signature candidates) from a hand-rolled `.tile` system to the shared `MetricCard`, deleting the duplicated tile/header/label CSS and routing their status pills through the card's own badge instead of a nested `Badge` widget (`internal/hud/frontend/src/lib/components/mills/MillStaffPanel.svelte`).
- Give `MetricCard` an optional `element` prop (`div` by default, plus `article`/`section`/`li`) so a card that is a self-contained unit keeps its semantic boundary instead of flattening to a bare `<div>`, and stop its badge from wrapping mid-text by restoring the `white-space: nowrap` the standalone `Badge` widget had (`internal/hud/frontend/src/lib/components/shared/MetricCard.svelte`).
