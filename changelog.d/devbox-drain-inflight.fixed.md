- Drain Kubernetes devbox work on SIGTERM through response delivery and async terminal polling, refuse new calls with retryable errors, and protect active sandboxes (`cmd/mcp-devbox`, `pkg/lifecycle`). Coordinate shared WebSocket/HTTP child shutdown and readiness (`cmd/custom-server`), retain preStop with an effective 1860-second grace (`k8s/base/servers/devbox/deployment.yaml`, `k8s/base/kustomization.yaml`), and document timeout tuning in `README.md`.
