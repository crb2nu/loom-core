- **Mills Overview rebuilt around resolved model wiring** (`internal/hud/frontend/src/lib/components/mills/OverviewPanel.svelte`, `internal/hud/frontend/src/lib/utils/millsWiringHelpers.ts`, `internal/hud/frontend/src/lib/stores/mills.svelte.ts`): the overview showed a wall of unclickable counters while the fleet's actual model routing — judge on `or/kimi-k3` via LiteLLM with a fallback chain, weaver on FlexInfer, per-stage Codex models (`gpt-5.6-terra`/`gpt-5.6-sol`), and the LiteLLM council lenses — was invisible. The panel now leads with a "Loom wiring" card: a stage-routing table (stage → agent → model with a policy/env/default source chip), judge + weaver rows rendering the fallback chain as a visible degrade arrow list plus max-tokens, a council row with backend-colored lens chips (a `fake`-backend lens flags as a warning chip), and LiteLLM-configured / LLM-gates / tiebreaker status chips. Every backend/agent gets a consistent Badge variant from the shared union (LiteLLM/FlexInfer/Anthropic/Codex/Claude distinct but calm; fake/misconfig = warning). It fetches `GET /api/mills/wiring` through a dedicated slow (60s) poller that catches up on window focus, normalises the payload at the fetch boundary (every array `?? []`, nested structs defaulted), and — until the endpoint lands on an operator — falls back to a committed sample fixture behind a "sample data" badge (404/SPA-catch-all → sampled; 503 → disabled; hard error keeps the last-known wiring). The KPI wall is replaced by a compact three-tile strip (cost/merged, gate pass, escalation rate) sourced from the existing KPI snapshot and deep-linking to the Telemetry panel, plus a one-line operational-state strip (behavioral state, degraded dependencies, rolling-24h pipeline budget). The in-flight/queue banner, autonomy ready/pause control, and Spinning Room card are retained. Store changes are purely additive (a new `wiring`/`wiringSampled`/`wiringError`/`wiringLoading` slice + `fetchWiring`, off the shared 15s `fetchAll` tick).
