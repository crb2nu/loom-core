Codex spawns no longer fail with "agent execution incomplete: N tool call(s) still open" when Codex abandons parallel command items at a clean turn end. The parser now sweeps still-open tool calls on `turn.completed`, handles terminal `item.updated` events, and keeps completions idempotent; the completion guard still fails closed on real mid-turn truncation and now names the open calls in its error. Root-caused from captured stream `spawn-d9889e05e2e9` (escalations #365/#362/#361), where a successful implement run that committed and pushed was failed three times and escalated.
