- **Report why a Codex turn died instead of how many tool calls it left open** (`internal/hud/spawn.go`, `internal/hud/spawn_codex_parser.go`): the spawn finalizer now consults the parser's terminal reason before the open-tool-call guard, the Codex terminal reason survives events emitted after `turn.failed`, and a stream that ends mid-batch after a fatal `error` event names that error alongside the open-call count. A clean `turn.completed` still sweeps stragglers, and unexplained truncation still fails closed.
