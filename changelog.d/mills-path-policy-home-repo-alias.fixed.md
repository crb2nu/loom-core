- Mills `path_policy` gate: an item whose `TargetProject` names the operator's own repository (bucket-qualified `services/loom-core` or bare `loom-core`) is now judged as a home-repo item against the global protected paths, instead of failing closed as an unknown foreign target. Four hand-filed items escalated on 2026-09-12 after successful implement attempts for this reason; the runner now passes its home project to gates as `StageInput.HomeProject`.
