- Changed `mcp-weaver`, `mcp-mentatlab`, `mcp-jobsearch`, `mcp-linkedin`, and `mcp-devbox` to start in a visible degraded state when backend configuration is absent and report `NotConfigured` from affected tool calls, with one startup WARN naming exactly the missing variable(s) (weaver names `FLEXINFER_URL`/`MCP_HUB_URL` individually and re-checks per call; linkedin names the credential its `LINKEDIN_MODE` needs). Each server package now proves initialize → tools/list → tools/call in-process through the shared `internal/mcptest` driver, and the all-servers smoke test no longer gates these five behind env vars or a docker CLI.
