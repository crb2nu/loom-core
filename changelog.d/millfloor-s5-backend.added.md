- **Mill-floor S5 backend — nil-safe backlog wire + clickable MR base URL** (`cmd/loom-mills-operator/handlers_backlog.go` `coerceBacklogList`/`coerceBacklogArrays`, `cmd/loom-mills-operator/server.go` `withGitLabBaseURL`/`gitlabWebBaseURL`, `cmd/loom-mills-operator/handlers_status.go`, `cmd/loom-mills-operator/main.go`): two small backend polish items the mill-floor HUD views (Warps/Shuttles/Sparks/Bolts) build on. **B2** — `GET /api/mills/backlog` (and the single-item `GET /api/mills/backlog/{id}` + create echo) now coerce nil arrays to `[]` at the encode boundary: `BacklogItem`'s array fields are untagged (PascalCase keys, no `omitempty`) so a nil `Labels`/`Dependencies`/`Slices` — and per-slice `Files`/`Tests` — serialized as `null`, which forced every client to special-case it and crashed the HUD warp view on an empty beam. The `omitempty` fields (`Slice.ParallelWith`, `SuccessCriteria.*`, `ItemPolicy.ProtectedPathsTouched`) are deliberately left untouched so they stay omitted rather than becoming a noisy `[]`. **B1** — `GET /api/mills/status` now carries `gitlab_base_url`, the GitLab instance web base (scheme+host, derived from the configured REST API URL by stripping `/api/v4`), so the HUD can build a clickable MR link `<gitlab_base_url>/<BacklogItem.TargetProject>/-/merge_requests/<iid>` from data it already holds — no per-run schema change, and it works across repos because the per-item `TargetProject` supplies the path. Empty string when no GitLab API URL is configured, so the HUD degrades to an iid chip. Pinned by table-driven tests for the URL derivation and coercion carve-out plus endpoint tests proving empty-state responses encode as `[]` and the status body exposes the base.
