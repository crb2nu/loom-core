- Local agents reach the serial merge queue via the loomd HUD proxy: new `GET /api/mills/merge-queue` open read (active candidates + per-lane depth) on the operator and HUD, `POST /api/mills/merge-queue/enqueue` proxied through the HUD's admin gate so the operator token never lands in per-agent env, `mcp-gitlab` falls back to the direct merge only on the queue's policy-`disabled` outcome, and the shipping skills (landing procedure, ci-green-merge-loop, merge-readiness, mills-ops) document queue-first merging.
