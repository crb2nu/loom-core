- Mills research calls now pin prefix-cache routing per backlog item: `pkg/mills/clients/flexinfer.go` gained a per-request cache-key seam (`chatOptions.cacheKey`) and `WeaverClient.Research` sends `X-Flexinfer-Cache-Key: mills-item:<backlog-id>` (reusing `agentloop.HeaderCacheKey`), so on a multi-replica lane two items' journal-led prompt prefixes no longer compete for one replica's cache. Calls without a backlog id send no header; judge and council paths are unchanged. Documented in `docs/JOURNAL_ENGINE.md` (Routing).
