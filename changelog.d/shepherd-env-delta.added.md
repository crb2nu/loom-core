- Shepherd environment-delta predicate (B2): escalations are stamped with a failure-shape fingerprint (normalized evidence tail, pkg/mills/sigfp; migration 035), and relaunch proposals now require cohort evidence — a sibling item with the same fingerprint merged after the escalation — while a recently-active fingerprint suppresses relaunch entirely. Unstamped legacy runs keep the v1 aged-and-unreachable reason during the transition.
