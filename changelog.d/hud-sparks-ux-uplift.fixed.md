- **HUD Sparks view leads with what needs a human and its columns line up again** (`internal/hud/frontend/src/lib/components/mills/SparksPanel.svelte`, `mills.svelte.ts`, `App.svelte`): the table defaults to open sparks (one row per backlog item whose current state is escalated/paused) with an explicit *history* scope for every archived attempt, and the header count, nav badge, and floor spine all read that same open-spark number instead of the all-time history (the "Sparks 120" beside "0 sparks on the floor" contradiction). "Struck today" now counts only runs that ended since local midnight rather than the whole terminal archive. The Warp and Class cells were `display:flex` on the `<td>` itself, which fuses adjacent cells into one anonymous table cell and shifted every header one column left; flex now lives on an inner wrapper. The same td-as-flexbox defect is fixed in Bolts, Alerts, Catalog, Memory, and the Servers table. The failing-gate column says "outside a gate" once, quietly, for infra/transient/merge escalations instead of repeating "no failing gate recorded" down the column, and a run whose detail fetch fails is marked unavailable rather than re-spending the fetch budget every poll. The relaunch queue carries its count and scrolls inside a five-row box instead of pushing the table off-screen.
