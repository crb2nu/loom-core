- Declared `Success.Tests` entries with leading env assignments (the workspace's `GOWORK=off go test ...` convention) now execute in the tests stage instead of being silently skipped, and bare `go test` entries are normalized to `GOWORK=off` — the sandbox's `go.work` toolchain floor made bare invocations fail instantly (`FAIL test:0` in ~100ms), escalating runs whose code was green.
