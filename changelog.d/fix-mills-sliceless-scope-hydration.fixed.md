- Mills: slice-less backlog items (GitLab-issue imports, council plans whose slices declared no files) no longer run unscoped to `post_implement_gate` — the pipeline runner now hydrates file-bearing slice scope from the plan store right after the `plan_slice` stage (`pipeline.Runner.SliceHydrator`, wired via `clients.PlanClient.SliceScopeForPlan`), and the `plan_slice` spawn prompt instructs the agent to persist its decomposition with `agent_plan_slice_add`/`agent_plan_slice_update` instead of only printing it. Regression tests pin the escalation #332/#338 item shapes to the scope gate's advisory skip (`pkg/mills/gates/gates_test.go`) and the hydrated envelope to enforced pass/fail (`pkg/mills/pipeline/slice_hydration_test.go`).
