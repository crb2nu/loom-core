- **Backfill mills metrics test coverage to the full declared set** (`pkg/mills/metrics_test.go`, `pkg/mills/metrics.go`): the three metrics tests (registration, label arity, gatherability) had drifted ~26 metrics behind `metrics.go` — everything recent (take-up pattern harvests, regression gate, scope amendments, stage error classes, workflow start/selection/terminal counters, LLM token accounting, item-memory, research/council grounding guards) was untested. All three tables now cover every declared metric, with each vec exercised using its canonical label values so a label-arity regression fails before deploy. Also corrected the stale dashboard pointer in the `metrics.go` header comment (`loom-mills.json` → `services-loom-mills-dashboard.yaml`).
