- `mcp-mills`: mutations now accept the operator admin token under `LOOM_MILLS_OPERATOR_TOKEN` (the name loomd and the HUD environment already carry) as well as `LOOM_MILLS_TOKEN` / `LOOM_MILLS_ADMIN_TOKEN`, and the registry passes it through — `mills_backlog_update_state` and `mills_backlog_post` from the daemon-hosted server no longer fail NotConfigured while the credential sits one process up.
