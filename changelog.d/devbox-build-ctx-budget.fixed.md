devbox: the build-scoped context now exceeds the per-pod wait (35m > 30m). It was 15m, and since context deadlines compose by taking the earlier one, every cold sandbox build died at exactly 15 minutes with an error blaming the 30m pod wait — the generic git-clone image could never be built. Second leg of the 2026-08-30 Mills tests-stage outage (first leg: the watch-expiry fix in the previous release).
