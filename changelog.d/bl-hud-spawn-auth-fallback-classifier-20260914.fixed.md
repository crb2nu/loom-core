- Fix Claude spawn auth fallback in `internal/hud/spawn_auth_fallback.go`, the parser, account selector, lifecycle, supervisor recovery, credential installation, and metrics: classify stdout errors even at exit 0, rotate healthy OAuth accounts, and reserve one visible cross-mode relaunch under a durable UTC daily cap. Add retry persistence and the daily cross-mode cap in `internal/spawn/{types,auth,controller,store}.go`, CLI-captured regression fixtures, and the operator runbook in `docs/MILLS.md`.
