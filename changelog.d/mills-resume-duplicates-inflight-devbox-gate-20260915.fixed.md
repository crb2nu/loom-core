- Stop and drain stale run and baseline sandboxes before Mills resumes a tests stage, recording cleanup evidence on the attempt (`pkg/mills/pipeline/{runner,dispatcher}.go`, `pkg/mills/clients/devbox.go`). Cancel detached quality gates on request cancellation, fence sandbox reuse through cleanup, and wait for Kubernetes pod termination (`cmd/mcp-devbox/{quality_gate,manager,handlers}.go`, `internal/devbox/backend/k8s_runtime.go`).
