- Collapse the HUD audit drawer onto the shared `DetailDrawer` chrome (focus trap, Escape re-arm, backdrop, labelled ✕) and introduce a shared overlay stacking scale (`--z-drawer` / `--z-modal` / `--z-takeover` / `--z-grain` in `theme.css`), replacing the ad-hoc z-index values hand-rolled by `AuditDrawer`, `BoltArchive`, `ShiftReport`, `AndonMode`, and the `Modal` widget (`internal/hud/frontend/src/lib/components/shared/action/AuditDrawer.svelte`, `internal/hud/frontend/src/lib/components/mills/`, `internal/hud/frontend/src/lib/styles/theme.css`).
