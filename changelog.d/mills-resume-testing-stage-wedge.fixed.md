- Restart resumed Mills tests and hub attempts with durable attempt numbers and visible logs, retry rollout hub outages as bounded substrate failures, and cancel silent stages after the repository timeout plus grace (`pkg/mills/pipeline/{runner,preflight,autonomy_gate,error_class,failure_classifier}.go`, `pkg/mills/{reconciler,reconciler_auto_requeue,metrics}.go`, and `cmd/loom-mills-operator/main.go`).
