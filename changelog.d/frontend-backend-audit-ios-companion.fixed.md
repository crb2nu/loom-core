- **Reconnect the iOS companion's dead frontend/backend wiring** (`apps/loom-companion-ios/**`): APNs device tokens now reach `POST /api/mobile/v1/push/register` — the SwiftUI lifecycle injects the API client into `AppDelegate` (which held tokens arriving before pairing), so push registration works end-to-end instead of silently dropping every token. The handoff inbox is presented from the Work tab (deep link, Dashboard attention lane, or toolbar) with Accept/Reject wired to `POST /handoffs/{id}/accept|reject`, which previously had zero callers. Workflow detail gained Approve/Reject buttons on top of the existing `.workflowApprove`/`.workflowReject` endpoints, and both the buttons and the `loom://workflow/<id>/approve` deep link now decode the HUD's actual response shapes (`{workflow, events}` and the mutation acknowledgement) — the deep-link approve path had been failing its detail decode before it ever reached the approve call. Session-less agent rows are navigable to `AgentDetailView` (now pull-to-refresh), and `loom://agent/<id>` plus the SessionSummaryWidget tap land on the right agent instead of nowhere. The Mills widget's per-pipeline escalate deep link works on iPhone (the binding was passed only on the iPad layout). Fleet, Tasks, Sessions, lock-screen, and Mills widgets no longer render sample data from `getTimeline` — an unpaired install shows an explicit "Not paired" state rather than a fake live fleet. The shift report tolerates a `403` from `/api/patterns` (daemons predating that entry in the mobile allowlist) and says "patterns unavailable" instead of silently dropping pattern attribution. `WeaverAPI.status()` degrades 404/503 to the "Weaver is not configured" empty state, WeaverScreen uses the Loom design system, MillsScreen distinguishes auth failures from connectivity and renders a total outage at screen level instead of as an idle floor, and `PlanDetailView` gained pull-to-refresh.
- **Run the companion's unit tests** (`apps/loom-companion-ios/project.yml`, `Tests/LoomCompanionKitTests/Fixtures.swift`): `LoomCompanionKitTests` existed only as a SwiftPM target, so 400+ tests never ran under `xcodebuild`. Added the unit-test target and scheme entry (with `Fixtures/` as a folder reference and a `Bundle.module` fallback for the Xcode build) plus coverage for the new handoff, workflow-decision, and pattern-catalog paths.
- **Document the companion's real API surface** (`docs/MOBILE_COMPANION_API.md`, `docs/deep-links.md`): corrected the false claim that all non-`/api/mobile/v1` paths are denied — documented the companion allowlist (`mills`/`plans`/`weaver`/`aimodels`/`patterns`), that those surfaces return bare JSON with `null` for empty collections rather than the `{ok,data,meta}` envelope, and the five registered alerting/auto-fix mobile routes. Added the previously missing `docs/deep-links.md` catalogue that `DeepLink.swift` cites.
