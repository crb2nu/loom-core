- Mills tests stage: the CI-parity lint command now passes `--allow-parallel-runners`, so concurrent runs sharing one per-repo sandbox pod no longer collide on golangci-lint's start-up lock (21 attempts in 2026-09-06..12 escalated as code failures with no lint verdict). A residual "parallel golangci-lint is running" refusal degrades the check instead of failing it, and the failure classifier now treats sandbox pod-replacement waits and exec-start failures at the memory limit as transient/infra rather than code, so no substrate fault escalates as a defect in the diff. Kill-test evidence in `.loom/brainstorm-loom-core-mills-critical-improvements-2026-09-12.md`.
