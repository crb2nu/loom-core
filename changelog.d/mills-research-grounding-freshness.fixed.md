- Fixed the Mills research stage's consistent false-positive "hallucination" withholds: `ensureRepoRoot` now refreshes the operator's repo checkout to origin/main on every boot (the ready fast-path had frozen the PVC clone at first population — observed 3 months stale — so every recently added path failed validation), the grounding guard exempts file paths the item's plan slices declare (new files the slice will create are intent, not invention), and the research prompt's repository digest now uses the council's package layout instead of an alphabetical tree digest that truncated inside `changelog.d/` before ever showing `cmd/`, `internal/`, or `pkg/`. Also clamps MR titles to GitLab's 255-char cap (`ClampMRTitle`), fixing the mr-stage 400 on long council-minted backlog titles. (`cmd/loom-mills-operator/repo_root.go`, `pkg/mills/clients/research_grounding.go`, `pkg/mills/pipeline/dispatcher.go`)
