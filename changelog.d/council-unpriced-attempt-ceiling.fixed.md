- Mills council no longer charges a whole run's admission reservation ($15) when one gateway-routed participant times out. A reviewer, editor, or judge attempt on a known `oa/`, `or/`, or Anthropic model that returns no usage is now priced at its bounded worst case (prompt + `max_tokens` at the pinned ceiling rates); unknown models keep the reservation fallback. Live cause: a 90s `or/kimi-k3` reviewer timeout billed $15.00 three times in four days (COUNCIL-2026-08-30-180032, -08-31-000037, -09-02-000053) against runs that actually cost ~$0.72. The per-reviewer deadline is also raised 90s → 180s (reviewers stage envelope 3m → 4m): the parked local workhorse cold-loads for ~66s on the first reviewer call, which left the 90s window too short for the local lenses and forced 1/4 quorum failures.
