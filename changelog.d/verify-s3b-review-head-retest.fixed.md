- Re-test the branch head when `pr_self_review` pushes commits: a deterministic `tested_head` gate on `post_review_gate` compares the review's resulting head with the tests stage's `tested_sha` and re-dispatches `tests` for the new head (no implement or review respawn, no implement attempt consumed) before `mr`/`ci_watch` can ship an untested tree; `pr_self_review` records `pushed_commits` and `mills_pipeline_review_head_moved_total{outcome}` counts how often review rewrites the tree (`pkg/mills/pipeline`, `pkg/mills/gates`, `cmd/loom-mills-operator`).
