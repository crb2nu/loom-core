- **Mills cumulative git capture survives operator rollouts and reports why it did not run** — `pkg/mills/clients/spawn.go` now records a structured `git_capture` outcome (status, reason, refs, counts) on every terminal spawn plus a WARN log instead of eight silent `return`s, `pkg/mills/pipeline/dispatcher.go` adds `SpawnContextResumeClient` so a stage re-attached after a pod roll still diffs `origin/<base>...origin/<branch>` (the bare `Resume` had no coordinates and captured nothing), and `pkg/mills/gates/nonempty_diff.go` names a dead capture in its failure reason rather than asserting the agent did no work over branch commits the pipeline could not see (issue #224).
