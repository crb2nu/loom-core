- **Plan objective + populated/rendered slice connective tissue** (`pkg/agentcontext/schema_plan.go`, `pkg/agentcontext/svc_plans.go`, `cmd/mcp-agent-context/tools_plan.go`, `pkg/mills/council/{editor,backlog_mutator}.go`, `pkg/mills/clients/{council,council_proposals,council_openai,council_anthropic,plan_spin}.go`, `pkg/mills/spin/spin.go`, `internal/hud/bridge/agent_plans.go`, `internal/hud/frontend/src/lib/utils/plansHelpers.ts`, `internal/hud/frontend/src/lib/components/{PlansPanel,PlansComparePanel}.svelte`): plans now carry a synthesized `objective` (the end-state + through-line connecting their slices, distinct from the raw brief in `spec_doc`), and the Spinning-Room producer now fills each slice's connective tissue. The council editor's `## Backlog Proposals` JSON contract gained a top-level `objective` key and per-slice `depends_on` / `interface_contracts` / `acceptance_criteria` (all optional, in the cached prompt prefix — extra fields, no extra round-trip). The spin threads the objective onto the draft plan and forwards each slice's tissue to `agent_plan_create`; `depends_on` is emitted by slice NAME and the plan store resolves names to minted `slice_id`s (self/unknown edges dropped). `agent_plan_create` accepts `objective` + documents the seed-slice tissue fields; `agent_plan_update` accepts `objective` so older/sparse plans can be enriched in place. The HUD plan detail drawer shows the objective prominently near the title and, per slice, the goal plus inline `depends on #1, #3` and `provides: <interface contract>`; the compare view shows each competing draft's objective. All fields are additive + null-safe — plans authored before this render cleanly with nothing, never `undefined`. Mills does NOT yet sequence pipeline dispatch by `depends_on` (the DAG edges now exist + render; sequencing is a separate MR).
