S1c crash-window observer retries transient observation-transport failures (kubectl calls killed or timed out during crash churn) on the next poll tick instead of aborting the window; the sampling-gap contract remains the fail-closed arbiter, retried attempts are recorded in evidence, and a gap breach after retries names the underlying cause. Both v5 gate attempts died 2/3 on a single such call.
