- Bound the agent-context write-path embed fallback with fail-closed thresholds: writes now return a typed `ErrEmbedderDegraded` instead of silently persisting deterministic fallback vectors once fallback usage exceeds a configurable windowed ratio (default 20%) or embedding fails continuously past a configurable duration (default 30m, the observable effect of the circuit breaker staying open). Adds `agent_context_embed_fallback_ratio` gauge plus write-attempt/fallback/rejection counters, env-tunable via `AGENT_CONTEXT_EMBED_MAX_FALLBACK_RATIO`, `AGENT_CONTEXT_EMBED_MAX_CONTINUOUS_FAILURE`, `AGENT_CONTEXT_EMBED_DEGRADED_MIN_SAMPLES`, and `AGENT_CONTEXT_EMBED_DEGRADED_WINDOW` (`pkg/agentcontext/embed_degradation.go`).
