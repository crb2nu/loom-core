- **Ship the `helm` CLI in the custom-server image so the hub's helm MCP server works** (`Dockerfile.custom-server`, `Dockerfile.custom-server.local`): `mcp-helm` shells out to `helm`, but the runtime stage only installed `kubectl`, so every tool on the loom-hub `helm` server failed with `exec: "helm": executable file not found in $PATH`. The runtime stage now also installs helm, pinned to the 3.19 line — Flux's helm-controller writes Helm 3 `sh.helm.release.v1` storage secrets, so the CLI has to stay on the same major line as the writer. This completes the fix started by the kubeconfig/ServiceAccount change: the in-cluster kubeconfig was necessary but not sufficient. `mcp-flux` is deliberately untouched; it probes for a `flux` binary but falls back to the Kubernetes API.
