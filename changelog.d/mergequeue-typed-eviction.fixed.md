- Merge-queue evictions are typed verdicts: head_moved reconstructs the runner-rewindable SHA-mismatch error (re-gate + re-prove the successor instead of escalating), other reasons classify per-reason instead of blanket code-class, and an evicted run may re-enter the queue through full enqueue-time re-authorization (migration 031: one active candidate per run) — ending the three no-op merge retries that replayed a settled eviction.
