- Stop Mills tests immediately when the devbox baseline oracle proves substrate failure, preserving retryable, free-retry infra metadata and the `devbox_baseline` dependency through escalation. Prevent post-tests rewinds and stop consecutive identical non-transient stage failures, with normalized signatures in retry logs (`pkg/mills/pipeline/runner.go`, `error_class.go`, `failure_classifier.go`, `escalate.go`, and regression tests in `runner_test.go`).
