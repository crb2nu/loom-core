- **HUD Factory shows what each shuttle is actually doing** (`internal/hud/frontend/src/lib/components/mills/{FactoryPanel,ShuttleBoard,PressLanes,OverseerStrip}.svelte`, `lib/utils/shuttleBoardHelpers.ts`, `lib/stores/mills.svelte.ts`): the loom canvas keeps the rhythm; beneath it a **shuttle board** now draws one lane per active pipeline run with the warp (priority, title, repo), a nine-pick stage track with the current pick lit, how long the shuttle has been observed in that pick (DELAYED on the same fuse the departures board uses), who is weaving it (model and agent from the stage record), the gate tally, spend, the MR once one exists, and the last log line of the newest stage record, labelled when it belongs to the last finished pick rather than the in-flight one. Stage/gate detail is fetched per lane on a budget of eight, re-read when a run advances and every 45 seconds while it sits, and the open drawer's run is never disturbed. The **press** is now a lane list of the serial merge queue in queue order (MR chip, backlog item, rebasing / re-proving / pressing, attempts, age) plus the last three settled with eviction reasons, instead of a bare depth. An **overseers** strip shows the mill staff (groomer, sentinel, foreman, shepherd) with last tick age, what the tick did, and why one is quiet (paused, suppressed, errored, silent). The sparks instrument carries the 24h infra-vs-real split; the header shows autonomy blockers and a degraded or reconnecting feed; the board header shows when the council last sat and when the last bolt was pressed. The departures board becomes the arrivals log so no run is drawn twice on the floor.
