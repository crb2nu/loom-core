- **Backlog drawer escalation triage: per-item run history + inline failure evidence** (`cmd/loom-mills-operator/handlers_pipeline.go`, `pkg/mills/store/dao_pipeline.go`, `internal/hud/frontend`): `GET /api/mills/pipeline/runs?backlog_id=X` now filters server-side (newest-first, `limit=` default 20, served off `idx_pipeline_backlog`) — previously the param was silently ignored and the caller got the unrelated active-run union. The HUD backlog drawer fetches this list on open, so the "why is this item escalated?" cross-link keeps working after the culprit runs age out of the 7-day/50-run terminal poll window, and its attention block now inlines the newest failing stage plus the tail of its log (or its failed gates' reasons) — the common triage ends in the drawer instead of three drill-downs deep. On an older operator the response is filtered client-side to the item's own rows, so the drawer degrades to exactly the prior behavior.
