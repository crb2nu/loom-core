- Bound streaming stderr capture to an 8 KiB head and less than 64 KiB of tail buffers in `internal/devbox/backend/stream_exec.go`, spilling oversized lines to temporary files until eviction or cleanup so gates retain complete last-20-line tails and existing line-based truncation. Returned strings and whole-line callbacks still allocate their complete output. Add optional `StderrHead` in `result.go` and populate it in `docker.go`, `k8s_runtime.go`, and `harvester_vm.go`, with regression coverage in `result_test.go` and `stream_exec_test.go`.
