- **Mill-floor Bolts view (S4)** (`internal/hud/frontend/src/lib/components/mills/BoltsPanel.svelte`): the take-up roll — merged runs woven into the week's cloth. Replaces the S0 placeholder with an inline "tartan of the week" strip (7 days, one pick per terminal run) and a merged-runs `DataTable` driven off `millsStore.fetchArchiveRuns`/`boltRuns` (never `pipelineHistory`, which feeds the loom's weave diff). Each row shows its `!<IID>` bolt chip (click-to-copy until backend B1 lands the clickable MR URL), plan/pattern-book attribution joined via `BacklogID`→`PlanID`, cost, merged-ago, and a per-run `seededPattern` swatch that matches the live loom. A totals row surfaces week bolts/spend plus the `pipeline_merged_runs` north-star odometer (guarded for absent KPI keys). "Export tartan" and "Shift report" reuse the existing `BoltArchive`/`ShiftReport` modals (offline self-contained SVG export preserved); rows open the shared `PipelineRunDetail` drawer. Loading/error/empty are handled exclusively via `PanelShell`; the archive refreshes on a dedicated 60s `createPoller` (explicit initial fetch, no initial tick) alongside the 15s KPI cadence.
