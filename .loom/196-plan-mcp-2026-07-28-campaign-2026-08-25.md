# Implementation plan: MCP 2026-07-28 fleet campaign (Phases 0–1)

- **Date**: 2026-08-25
- **Status**: kill-test S0 in flight; slices staged for post-smoke enqueue
- **Lineage**: chosen via `.loom/brainstorm-next-features-unlock-2026-08-25.md` (recommendation C2); technical basis `.loom/195-research-mcp-2026-07-28-gap-roadmap-2026-08-15.md`
- **Stop-line (operator decision 2026-08-25)**: Phase 1 — leaves negotiate 2026-07-28 + structuredContent through the scaffold choke point; **hub fabric untouched**. Phases 2–3 are explicitly out of scope; monitor and adapt after Phase 1 lands.

## S0 — kill-test: **PASSED 2026-08-26T01:24Z**

Assumption under test: Mills can work `libs/mcp-go` end-to-end. **Proven**: smoke MR libs/mcp-go!11 authored AND merged by the operator's group bot, green pipeline; item `bl-mcpgo-smoke-readme-20260825` state `merged`.

1. ✅ Disconfirming checks: spawn clone token (devbox/gitlab-creds, root PAT) reads libs/mcp-go; repo has `.gitlab-ci.yml` on main.
2. ✅ Enablement landed by operator rescue of scope-escalated draft !1631 (rebase-port onto post-split `spawn_lifecycle.go`; harvester-vm clone-path regression fixed in the same MR).
3. ✅ Access grant: libs⇄services group share raised 20→40 AND libs/mcp-go project-level share services@40 (group-bot = operator `GITLAB_TOKEN` proves branch create/delete; token scope `api`, expires 2027-06-27). Gotcha: the `loom-mills-gitlab-group` secret's only key is `api-token` — probing `.data.token` yields an empty credential and misleading 404s.
4. ✅ Rollout + smoke, five attempts, one real gap per attempt:
   - **A1** — clone 404: pre-merge image + post-merge env (MR pipelines push SHA tags only; timestamp tags come from MAIN pipelines; Flux image scan interval 30m — nudge via `reconcile.fluxcd.io/requestedAt`).
   - **A2** — `post_implement_gate` / `path_policy` fails closed on unknown repos → gitops!628 `protected_paths_per_repo` overlay. Proved clone/implement/branch-push on the libs repo.
   - **A3** — tests stage: devbox buildah pod cloned `…/services/mcp-go.git` (build path + manager start path never thread GitProjectPath) → loom-core!1722: `/workspace`-derived project paths + `joinRepoURL` boundary-segment dedup (rollout-order safe) + `DEVBOX_K8S_GIT_BASE_URL` group-neutral. NOTE: the devbox server runs the **custom-server** image, not loom-core — verify rollouts against the right ImagePolicy.
   - **A4** — `nonempty_diff.capture_unavailable`: fresh run over the already-pushed branch; spawn runs have no `working_dir` capture coordinate so the branch-diff union never runs → false empty diff. Filed `bl-mills-spawn-capture-branch-diff-fallback-20260826`; unblocked by deleting the stale smoke branch.
   - **A5** — **full lane green**: plan_slice → research → implement → gates → tests (devbox sandbox on libs) → MR !11 → merged 01:24Z.
5. ✅ Escalation issue #608 closed with the resolution trail.

**Wave 1 FILED 2026-08-26**: `bl-mcpgo-leaf-version-negotiation-20260825` (queued) → `bl-mcpgo-structured-content-20260825` (queued, deps=[leaf-version] — dependency hold verified on filing).

## Waves 1–2: COMPLETE 2026-08-26 (all autonomous merges)

| Item | MR | Landed |
|---|---|---|
| Phase 0.1 leaf version negotiation | libs/mcp-go!12 | 01:44Z |
| Phase 0.2 structuredContent + outputSchema | libs/mcp-go!13 | 02:03Z |
| Phase 1.4 server/discover + handshake-less | libs/mcp-go (wave 2) | ~05Z |
| Phase 1.5 deterministic ordering + CacheableResult | libs/mcp-go (wave 2) | ~05Z |
| Phase 0.2b loom-core consumer half (plan_reader prefers structuredContent, SDK bump) | loom-core!1726 | 07:01Z |
| capture branch-diff fallback (S0-A4 finding) | loom-core!1725 (operator rescue of scope-escalated draft; scope reach into main.go/gitlab_repo.go was genuine) | 18:50Z |

**Incident during wave 1 (2026-08-26 ~22:36→05:5x)**: the !1631 fail-closed spawn gate rejected every HOME-lane spawn — Mills' dispatcher sends the bucket-qualified home project (`services/loom-core`) while `SPAWN_PROJECTS` lists bare names; exact-match-only admitted libs/mcp-go (via `spawnProjectPaths`) but not home. Wave 1 sailed while the home lane was dark; first hit was the capture item. Fix loom-core!1724 (bare candidates admit bucket-qualified forms; regression test covers the dispatcher shape !1631's fixtures never probed — the untested-spawn-shape lesson, again). Home lane validated by !1726 merging autonomously.

**Wave 3 FILED 2026-08-26**: `bl-loom-cimd-daemon-oauth-20260826` (labels mcp-modernization+**security** → human-review MR, NO auto-merge), `bl-mcpgo-remove-legacy-sse-20260826`, `bl-loom-remove-sse-surface-20260826` (deps=[mcp-go half], cross-repo hold). Wave 3 completes the Phase 0+1 stop-line; Phases 2–3 stay out of scope pending a fresh plan cycle.

## Wave 3 outcome (2026-08-26 evening)

- **7a SDK sse.go removal: ✅ merged** (autonomous, libs lane).
- **7b loom /sse surface: ⛔ correctly BLOCKED — roadmap premise falsified.** The implement agent's mandated disconfirming grep found `k8s/base/servers/mcpo/configmap.yaml` with **22 live /sse endpoints** (mcp-agent-context, mcp-github, mcp-grafana, mcp-youtube, … all on `custom-server:latest`): removing the handler would break mcpo. It pushed no artificial diff and stopped — the escalation IS the report. `.loom/195`'s "SSE unused" inventory claim was true for the SDK, false for the loom surface. **Prerequisite before 7b can run**: migrate mcpo consumers to Streamable HTTP (or retire mcpo) — real work, its own future item; the 12-month deprecation clock leaves room. Item awaits operator close (superseded-by-prerequisite).
- **Item 6 CIMD**: v1 escalated on `path_policy` — protected paths must be PRE-DECLARED via `ItemPolicy.ProtectedPathsTouched`, which the v1 payload omitted (no item-update endpoint exists → superseded). **v2 filed** with the four oauth files declared + `require_human_review: true`; its MR pauses for operator review by design. v1 + 7b sit in the operator's widen-vs-close queue (deliberately human decisions).

**Campaign score at the stop-line**: 7 of 8 slices landed autonomously end-to-end; 1 blocked on a falsified premise (the system refusing to break a live consumer is a win, not a miss); CIMD handed to human review as designed. Operator queue: review CIMD v2's MR when it appears; close v1-CIMD and 7b; decide the mcpo migration's future.

Side quest (operator request 2026-08-26): pre-flip hygiene — `bl-mills-enable-libs-mcp-go-20260815` settled `escalated→merged` by the ghost-sweep's branch pass after two one-row repairs (expire `escalation_sweep_state.recheck_after`; `mr_iid` kept schema-honest NULL) + operator pod bounce (the recheck cache is process-local; startup runs an immediate sweep pass). Lesson: an `mr_iid` WITHOUT durable stage-artifact provenance makes an item invisible to BOTH sweep passes (`ListEscalatedWithMR` demands a successful mr/ci_watch/merge/cleanup stage project-key artifact; `ListEscalatedWithoutMR` excludes MR-bearing runs). Follow-ups worth filing: escalator stamps durable provenance for rescue MRs; MR-merged webhook (!1560 decision) for instant external-merge settles.

**Access-grant blast radius**: every services-group member now inherits up to Maintainer in `libs/` (capped by their services level). Members today: root + the two group bots. Kill-switch: revoking the `loom-mills-gitlab-group` token still severs Mills from both groups; per-group granularity was traded for zero new token plumbing. Revisit if a dedicated `loom-mills-libs` token ever becomes worth the third-key wiring (cf. gitops bootstrap: THREE keys).

## Slice plan (staged enqueue; Dependencies chain, plan store NOT used — emitter has no dep awareness)

Lane discipline: SDK slices serialize on `libs/mcp-go` (one lane, no scope overlap); loom-core consumer slices depend on the SDK version bump. All specs demand only repo-sandboxed evidence (S4 lesson: no live-cluster proofs). Budgets: leave default (policy `max_usd_per_run` = 5; never author above it).

### Wave 1 — file after smoke passes
1. **`bl-mcpgo-leaf-version-negotiation`** (libs/mcp-go, P2) — consult `SupportedProtocolVersions` in `handleInitialize` (`server.go:346-390`): echo client's version when supported, reply highest-supported otherwise, never default-forward; WS client handshake follows; emit/read `MCP-Protocol-Version` header on Streamable HTTP; fix README/docs drift (roadmap item 3 folded in). Tests: table-driven negotiation matrix.
2. **`bl-mcpgo-structured-content`** (libs/mcp-go, P2, deps: [1]) — add `outputSchema` to `Tool`, `structuredContent` to `CallToolResult`; `JSONResult` dual-emits structured JSON + TOON text fallback. Retires the TOON array-column data-loss class at the contract level. Tests: round-trip on an array-valued column that TOON drops today.

### Wave 2 — file when wave 1 merges
3. **`bl-loom-structured-content-consumers`** (loom-core, P2, deps: [2]) — bump `libs/mcp-go` in go.mod; hub/bridge/mills consumers prefer `structuredContent` when present (`plan_reader.go` first — the documented data-loss victim); keep TOON fallback path.
4. **`bl-mcpgo-discover-handshakeless`** (libs/mcp-go, P2, deps: [2]) — Phase 1 item 4: accept per-request `_meta` (protocolVersion/capabilities), treat missing-initialize as valid, implement `server/discover` (also the STDIO back-compat probe), stamp `resultType:"complete"` on all results.
5. **`bl-mcpgo-cacheable-lists`** (libs/mcp-go, P2, deps: [4]) — deterministic tool ordering + `ttlMs`/`cacheScope` on list responses.

### Wave 3 — file when wave 2 drains
6. **`bl-loom-cimd-daemon-oauth`** (loom-core, P2, independent) — CIMD alongside DCR in daemon OAuth (`internal/daemon/oauth.go`); client-side RFC 9207 `iss` validation; DCR kept for the 12-month window.
7. **`bl-mcpgo-remove-legacy-sse`** (libs/mcp-go, P3, deps: [4]) + **`bl-loom-remove-sse-surface`** (loom-core, P3, deps: [7a, 3]) — delete unused `mcp-go/sse.go`; remove `custom-server` /sse surface.

## Monitoring / adapt triggers

- Monthly proxy fallback kill-test (from .loom/195 §5) stays scheduled: newest Claude Code + Codex over stdio `loom proxy` round-trip. Vendor drops fallback → item 4 becomes P1 and jumps the queue.
- If two consecutive SDK slices escalate on scope or spec-capability grounds, pause the lane and re-slice with the escalation evidence (immune-system discipline) instead of relaunch-grinding.
- Phase 2/3 (tasks extension, MCP Apps `_meta`, MRTR elicitation, stateless hub fabric, official-SDK spike) re-enter via a fresh brainstorm/plan cycle after Phase 1 lands — not before.

## Handoff

- Backlog IDs above are the source of truth once filed; this doc mirrors intent.
- Linked: `.loom/brainstorm-next-features-unlock-2026-08-25.md`, `.loom/195-research-mcp-2026-07-28-gap-roadmap-2026-08-15.md`, loom-core !1631.

## CAMPAIGN CLOSED at the stop-line — 2026-08-26T21:52Z

CIMD landed via loom-core!1730 after operator review (2026-08-26): the v2 item ran with
`ItemPolicy.ProtectedPathsTouched` pre-declared; the security label held the MR un-merged;
the operator-directed review found the implementation EXCEEDS spec (dial-time public-IP
re-validation defeats DNS rebinding; CGNAT/special-purpose ranges excluded; strict
content-type/size/single-object fetch discipline; redirect_uri re-checked at token time;
latent state-encoding bug fixed in passing). One noted gap: `validateAuthorizationIssuer`
is implemented+tested but has no production caller until the daemon grows an OAuth-client
flow. Item self-settled via the IID sweep pass in ~3 min — proving settles are autonomous
when runs carry stage provenance (the earlier manual settles compensated for
provenance-less rescue MRs only).

Final ledger: **8 of 8 dispositioned** — 7 merged (6 fully autonomous + CIMD via designed
human review), 1 correctly refused on falsified premises (7b; mcpo /sse migration is the
prerequisite follow-up). Mechanism lessons banked: `require_human_review` gates ADMISSION
(use the security label for review-at-merge instead); backlog updates = CAS upsert
(POST /api/mills/backlog with current Revision).

Operator queue remaining: close v1-CIMD + 7b (widen-vs-close; CAS upsert or flip-day
scope-decision flags), decide the mcpo /sse migration item, and the shepherd allow-flag
flip (~08-27) — hygiene is clean on this campaign's side.
