apiVersion: v1
data:
  server.yaml: |
    always_allow:
        - jira_search
        - jira_get_issue
        - jira_list_projects
        - jira_get_project
        - jira_myself
        - jira_get_transitions
    categories:
        - devops
        - collaboration
    description: Atlassian Jira issue tracking (read-only)
    name: jira
    timeout: 60
kind: ConfigMap
metadata:
  labels:
    app: jira
    component: mcp-server
  name: jira-config
