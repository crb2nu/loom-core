apiVersion: v1
kind: Service
metadata:
  labels:
    app: confluence
    component: mcp-server
  name: mcp-confluence
spec:
  ports:
    - name: websocket
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: confluence
  type: ClusterIP
